---
title: "Functional biogeography"
author: "Orlando Acevedo-Charry"
date: "`r Sys.Date()`"
output:
  html_document: default
editor_options:
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Objective

How population-level ecological release scales to community-level trait assembly across a gradient of ecologically similar sites with declining species richness? 

![Conceptual framework illustrating three hypotheses for how ecological release at the population-level may scale to community-level patterns in functional trait space.](figures jpg/Figure 1 - Conceptual_arrows.jpg)

Species-poor communities may show expansion in functional space (\textbf{Hypothesis 1: Expansion}). Reduced interspecific competition on islands allows species to exploit peripheral regions of the functional space. Functionally distinctive species may increase in abundance or expand their niche, leading to higher trait variability. We expect increases in the ranges of trait dimensions and extreme trait values, particularly in traits contributing to individual survival and fitness. Some of these traits may include dispersal morphology (e.g., wing shape), foraging morphology (e.g., tarsus length), and dietary morphology (e.g., beak shape). 

Communities may show a shift in functional space rather than expansion (\textbf{Hypothesis 2: Shift}). Reduced predation on islands may drive species to modify behavioral and ecological strategies without necessarily expanding functional trait spaces, which can remain constrained by higher intraspecific competition ([MacArthur 1972](https://esajournals.onlinelibrary.wiley.com/doi/10.2307/1934090), [Terborgh 2023](https://onlinelibrary.wiley.com/doi/abs/10.1111/jbi.14530)). This shift may manifest as changes in the dominant trait dimensions, such as an increased use of lower vertical strata ([MacArthur 1972](https://esajournals.onlinelibrary.wiley.com/doi/10.2307/1934090)), or an increased reproductive niche breadth, resulting in a higher number of nest structures and nest locations in species-poor communities. Still, in this scenario, variance among trait values remains similar. 

Ultimately, these two hypotheses may be applied simultaneously. Communities may show functional space expansion and shift of combined functional traits through joint release (\textbf{Hypothesis 3: Expansion + Shift}). When competition and predation pressures are relaxed, we may expect a synergistic effect of functional trait space, resulting in combined expansion and shift. This could result in increased trait diversity (higher variance), different trait dominance (mean), and reorganization of community-level trait distributions. Mechanistically, this reflects niche expansion and niche shift, driven by changes in abundance, behavior, and ecological strategies, accompanied by reduced competition and predation ([MacArthur 1972](https://esajournals.onlinelibrary.wiley.com/doi/10.2307/1934090), [Herrmann et al. 2021](https://www.sciencedirect.com/science/article/pii/S0169534720303116)). 

To compare these hypotheses, we analyzed data on birds--a diverse taxonomic group for which functional traits are well described at the species level ([Tobias et al. 2022](https://doi.org/10.1111/ele.13898), [Sheard et al. 2023](https://onlinelibrary.wiley.com/doi/full/10.1111/geb.13783), [Moura et al. 2024](https://journals.plos.org/plosbiology/article?id=10.1371/journal.pbio.3002658), [Şekercioğlu et al. 2025](https://www.nature.com/articles/s41597-025-05615-3)) . Bird communities have also been a classic case study in ecological release and density compensation in mainland-island gradients ([Lack 1944](https://onlinelibrary.wiley.com/doi/abs/10.1111/j.1474-919X.1944.tb04092.x), [MacArthur 1972](https://esajournals.onlinelibrary.wiley.com/doi/10.2307/1934090), [Lack 1976 - see Orians 1976](https://www.science.org/doi/10.1126/science.194.4270.1153), [Cox & Ricklefs 1977](https://www.jstor.org/stable/3543330?seq=1), [Wright & Steadman 2012](https://doi.org/10.1111/j.1365-2699.2012.02754.x), [Sherry et al. 2020](https://doi.org/10.1093/auk/ukaa049)). Specifically, we compared two regions with known gradients of species richness and high levels of bird endemism that have been foundational to island biogeography: the Caribbean and Eastern Indo-Pacific meta-archipelagos.

## Packages

The packages used are

```{r packages, echo=TRUE}
# For code 1 - Islands eBird filtering
library(tidyverse); #manipulate data
library(auk); #eBird data filters
library(dggridR) #Spatiotemporal Subsampling
library(sf) # Simple Features, to encode spatial vector data
library(patchwork) # arrange multiple figures
library(funspace) # functional trait space
library(vegan) # betadisper for "expansion" tests
library(emmeans) # Estimated marginal means
library(pairwiseAdonis) # pairwise adonis for "shifts" tests
```

## Islands eBird filtering

We downloaded all eBird data up to January 2026. To reduce the size of this files and filter accordingly to our aim, we select species considered Sedentary or Resident that do NOT occupy specific primary habitats Sea, Wetland, Riparian, or Coastal (only "land birds"), using the Mobility and Habitat trait groups in [Şekercioğlu et al. (2025)](https://www.nature.com/articles/s41597-025-05615-3). 

### Functional traits across relaxed taxonomy

We combine the eBird taxonomy with different bird trait databases to generate a list of taxa to filter in eBird (the code could be names as `0_resident_landbirds_list.R`):

```{r eBird list matching taxonomy of functional traits, eval=FALSE}
# eBird at 2025 will be my taxonomy (to filter in the ebd file)
eBird_taxonomy <- read_csv("FunctionalTraits/eBird-Clements_v2025-integrated-checklist-October-2025.csv") |> 
  select(scientific_name = `scientific name`, 
         category)

# BIRDBASE is more updated than AVONET, including Distribution, Body Mass, Elevation, Habitat, Diet, Reproductive Behavior, Demography, and Mobility
birdbase <- read_csv("FunctionalTraits/BirdBase.csv") |> 
  # only sedentary or resident species NOT from the Sea, Wetland, Riparian, or Coastal primary habitat
  filter(`Mobility - Sedentary or Resident` == 1,
         `Habitat - Primary Habitat` != "Sea", 
         `Habitat - Primary Habitat` != "Wetland", 
         `Habitat - Primary Habitat` != "Riparian",
         `Habitat - Primary Habitat` != "Coastal") |>
  # adjust traits for functional space estimation
  mutate(ESI = log10(100 / (`Habitat - Habitat Breadth` * (`Diet - Diet Breadth` + 0.1))), 
         `Body mass` = log(`Body Mass - Average`), 
         `Clutch size` = ((`Reproductive Behavior - Max Clutch Size` + `Reproductive Behavior - Min Clutch Size`) / 2),
         `Number of broods` = ((`Reproductive Behavior - Max Number of Broods` + `Reproductive Behavior - Min Number of Broods`) / 2),
         `Nest location breadth` = str_count(`Reproductive Behavior - Nest Substrate`, "[A-Za-z]"), 
         `Nest type breadth` = ifelse(`Reproductive Behavior - Nest Type` == "", 0, str_count(`Reproductive Behavior - Nest Type`, ",") + 1))

# long format of birdbase to try and harmonize taxonomies
birdbase_long <- birdbase |>
  pivot_longer(    
    cols = c(`Latin (BirdLife_IOC_Clements_AviList)`,
             `HBW/BirdLife International (v9.1)`,
             `IOC World Bird List (v15.1)`,
             `eBird/Clements (V2024b)`,
             `AviList v1 2025`),    
    names_to = "taxonomy_source_birdbase",    
    values_to = "species_name"  
    ) |>
  filter(!is.na(species_name))

# AVONET

# the raw data from Tobias et al.
avonet <- read_csv("ELEData/TraitData/AVONET_Raw_Data.csv")

# long format of avonet to try and harmonize taxonomies
avonet_long <- avonet |>
  pivot_longer(
    cols = c(Species1_BirdLife, 
             Species2_eBird, 
             Species3_BirdTree, 
             eBird.species.group),
    names_to = "taxonomy_source_avonet",
    values_to = "species_name"  
    ) |>
  group_by(taxonomy_source_avonet, species_name) |>
  summarise(
    `Hand Wing Index` = mean(`Hand-wing.Index`, na.rm = TRUE),
    `Tarsus length` = mean(`Tarsus.Length`, na.rm = TRUE),
    `Wing length` = mean(`Wing.Length`, na.rm = TRUE), 
    `Beak length (nares)` = mean(`Beak.Length_Nares`, na.rm = TRUE), 
    `Beak length (culmen)` = mean(`Beak.Length_Culmen`, na.rm = TRUE),
    `Beak width` = mean(`Beak.Width`, na.rm = TRUE),
    `Beak depth` = mean(`Beak.Depth`, na.rm = TRUE),
    .groups = "drop"
  )

# TetrapodsTraits
tetrapods <- read_csv("FunctionalTraits/TetrapodTraits_1.0.0.csv") |>
  filter(Class %in% "Aves")

# long format of tetrapods to try and harmonize taxonomies
tetra_long <- tetrapods |>
  pivot_longer(    
    cols = c(Scientific.Name, 
             IUCN_Binomial),    
    names_to = "taxonomy_source_tetrapods",    
    values_to = "species_name"  
    )

# Join and summarize per "taxa" (it will include subspecies and groups in eBird)

ebird_functional_taxa <- eBird_taxonomy |>
  left_join(birdbase_long, by = c("scientific_name" = "species_name")) |>
  left_join(avonet_long, by = c("scientific_name" = "species_name")) |> 
  left_join(tetra_long, by = c("scientific_name" = "species_name")) |>
  filter(!is.na(scientific_name)) |>
  group_by(scientific_name) |>
  summarise(`log (Body mass)` = mean(`Body mass`, na.rm = TRUE), # this already have the log transformation from birdbase
            `Hand Wing Index` = mean(`Hand Wing Index`, na.rm = TRUE), 
            `Tarsus length` = mean(`Tarsus length`, na.rm = TRUE), 
            `Wing length` = mean(`Wing length`, na.rm = TRUE), 
            `Beak length (nares)` = mean(`Beak length (nares)`, na.rm = TRUE), 
            `Beak length (culmen)` = mean(`Beak length (culmen)`, na.rm = TRUE),
            `Beak width` = mean(`Beak width`, na.rm = TRUE),
            `Beak depth` = mean(`Beak depth`, na.rm = TRUE),
            `Diet breadth` = mean(`Diet - Diet Breadth`, na.rm = TRUE),
            `Habitat breadth` = mean(`Habitat - Habitat Breadth`, na.rm = TRUE),
            `EcoloSpeciIndex` = mean(ESI, na.rm = TRUE),
            `Verticality index` = mean(`Verticality`, na.rm = TRUE),
            `Elevational range` = mean(`Elevation - Elevational Range`, na.rm = TRUE),
            `Clutch size` = mean(`Clutch size`, na.rm = TRUE),
            `Number of broods` = mean(`Number of broods`, na.rm = TRUE),
            `Nest location breadth` = mean(`Nest location breadth`, na.rm = TRUE),
            `Nest type breadth` = mean(`Nest type breadth`, na.rm = TRUE),
            .groups = "drop") |>
  unique()
saveRDS(ebird_functional_taxa, "FunctionalTraits/eBird_func_taxa.RDS")
```

Note that we saved the `.RDS` file, which is an object in `R` to facilitate transferring across systems/computers and backup without running again the code.

### eBird filtering through the HiPerGator

Now we can apply the filter for resident "landbird species" in the two meta archipelagos. To use the ebd files (big files), we should decompress them from a `.tar` archive and a `.txt.gz` compressed file. We can use the Terminal for this, with the command `tar -0xf ebd_relJan-2026.tar ebd_relJan-2026.txt.gz | gunzip > ebd_relJan-2026.txt`, which uses a pipe (`|`) and a redirection (`>`) to the folder that will contain the `.txt` file (same as the `.tar`: `ebd_txt_files`). With that file extracted, we can run the next code in the Terminal. But even better, the use of [HiPerGator](https://docs.rc.ufl.edu/) at UF would facilitate this process, although it requires a "scheduler" (`sbatch`) file to run specific codes.

First, we download the ebd file from [eBird](https://ebird.org/data/download/ebd). The file is 220 GB in `.tar` (decompressed is almost 900 GB!). So, we use [Globus](https://docs.rc.ufl.edu/data_transfer/globus/) to transfer the downloaded `.tar` file to the HiPerGator. Once there, we create the `sbatch` for decompressing the `.tar` (`.txt.gz`) to `.txt`. We named this scheduler as `extract_ebd.sh`, located in the same folder `funbiog_eBird/ebd_txt_files` to be run.

```{r, eval = FALSE}
#!/bin/bash

#SBATCH --job-name=extract_ebd     # Job name 
#SBATCH --mail-type=END,FAIL          # Mail events 
#SBATCH --mail-user=oacevedocharry@ufl.edu     # Where to send mail 
#SBATCH --ntasks=1                    # Run on a single CPU 
#SBATCH --mem=4gb                     # Job memory request 
#SBATCH --time=12:00:00               # Time limit hrs:min:sec 
#SBATCH --output=extract_ebd_%j.log   # Standard output and error log 
pwd; hostname; date 

echo "Running decompress of ebd file on a single CPU core" 

tar -xOf ebd_relJan-2026.tar ebd_relJan-2026.txt.gz | gunzip > ebd_relJan-2026.txt

date
```

Once the file is created, we run through the Terminal (logged in the HiPerGator) as `sbatch extract_ebd.sh`. We can check the process with `squeue -u oacevedocharry`, with the status (`ST`) running indicated by `R`

```{r eval=FALSE}
[oacevedocharry@login7 ebd_txt_files]$ sbatch extract_ebd.sh 
Submitted batch job 37330555
[oacevedocharry@login7 ebd_txt_files]$ squeue -u oacevedocharry
             JOBID PARTITION     NAME     USER ST       TIME  NODES NODELIST(REASON)
          37330555 hpg-milan extract_ oacevedo  R       1:46      1 c0709a-s2
```

When finished, HiPerGator sent a message:

```{r eval=FALSE}
Job ID                  : 37330555
Cluster                 : hipergator
User                    : oacevedocharry
Group                   : rcstudents
State                   : COMPLETED (exit code 0)
Cores                   : 1
CPU Time Used           : 01:20:17
Wall Time Used          : 01:21:36
CPU Efficiency          : 98.39%
Memory Requested        : 4.00 GB (4.00 GB/node)
Memory Used             : 10.00 MB (10.00 MB/core estimated maximum)
Memory Efficiency       : 0.24%

```

This process of creating a scheduler to run complex codes will be used for all HiPerGator processes. For instance, for the filtering of the data from the `funbiog_eBird` folder. We generate the `R` code (`1_a_filtering_eBird.R`):

```{r 1_a_filtering_eBird, eval=FALSE}
########################################
###~ eBird data - Island filtering 2026 ~####
########################################

#Packages
library(auk); #eBird data filters
library(tidyverse); #manipulate data
library(dggridR) #Spatiotemporal Subsampling
library(sf) # Simple Features, to encode spatial vector data

taxa_etal <- readRDS("FunctionalTraits/eBird_func_taxa.RDS") |>
  dplyr::select(scientific_name) |> 
  as.vector()

#Define the geographic range
CaribbeanRange <- c(-91,8,-59,27)
IndoMalayRange <- c(90,-20,180,20)

#select the columns to extract (based on x$col_idx$name)
colsE <- c("observer_id", "sampling_event_identifier",
           "group identifier",
           "common_name", "scientific_name",
           "observation_count",
           "country", "state_code", "locality_id", "latitude", "longitude",
           "observation_type", "all_species_reported",
           "observation_date",
           "time_observations_started",
           "duration_minutes", "effort_distance_km",
           "number_observers")

f_ebd_Ca <- "ebd_txt_files/ebd_Caribbean.txt" #Temporal file to save the filtering eBird data (records)

f_ebd_IM <- "ebd_txt_files/ebd_IndoMalayan.txt" #Temporal file to save the filtering eBird data (records)

ebd_filtCarib<-auk_ebd("ebd_txt_files/ebd_relJan-2026.txt") %>%
  auk_bbox(CaribbeanRange) %>% #W, S, E, N
  auk_year(c(2015:2025)) %>%
  auk_protocol(c("Traveling", "Stationary")) %>%
  auk_distance(distance = c(0,5)) %>%
  auk_duration(duration = c(0,300))%>%
  auk_complete() %>%
  auk_filter(f_ebd_Ca, overwrite=T, keep = colsE)

ebd_filtIndoM<-auk_ebd("ebd_txt_files/ebd_relJan-2026.txt") %>%
  auk_bbox(IndoMalayRange) %>% #W, S, E, N
  auk_year(c(2015:2025)) %>%
  auk_protocol(c("Traveling", "Stationary")) %>%
  auk_distance(distance = c(0,5)) %>%
  auk_duration(duration = c(0,300))%>%
  auk_complete() %>%
  auk_filter(f_ebd_IM, overwrite=T, keep = colsE)

# and with read_ebd I apply another filter to do not repeat records from groups
ebd_Caribbean <- read_ebd(f_ebd_Ca) |>
  mutate(Meta.Archipelago = "Caribbean")
ebd_IndoMalayan <- read_ebd(f_ebd_IM) |>
  mutate(Meta.Archipelago = "IndoMalayan")

# Function to convert time observation to hours since midnight
time_to_decimal <- function(x) {
  x <- hms(x, quiet = TRUE)
  hour(x) + minute(x) / 60 + second(x) / 3600
}

#Effort data
#Caribbean data
Carib_eff <- ebd_Caribbean |>
  mutate(
    # I don't want count in X, to convert to NA
    observation_count = as.integer(observation_count),
    # effort_distance_km to 0 for non-travelling counts
    effort_distance_km = if_else(observation_type == "Stationary",
                                 0, effort_distance_km),
    # convert time to decimal hours since midnight
    time_observations_started = time_to_decimal(time_observations_started),
    hour_sampling = round(time_observations_started, 0),
    # split date into year, month, week, and day of year
    year = year(observation_date),
    month = month(observation_date),
    week = week(observation_date),
    day_of_year = yday(observation_date),
    # temporal aggregation - month
    Time.t = case_when(year == 2015 ~ month,
                       year > 2015 ~ month+(12*(year-2015)))) |>
  filter(number_observers <= 10,         #Only list with less than 10 observers
         effort_distance_km <= 5,        #be sure of distance effort
         duration_minutes %in% (0:300), #be sure of duration effort
         !is.na(observation_count))      #only records with abundance estimation

Checklists_with_more_SppC <- Carib_eff %>%
  dplyr::select(checklist_id, scientific_name) %>%
  group_by(checklist_id) %>%
  summarise(N_species = n()) %>%
  filter(N_species >= quantile(N_species, 0.25)) # minimum number of species per sampling - 1st Quartile

Caribbean_cooc <- Carib_eff[Carib_eff$checklist_id %in% Checklists_with_more_SppC$checklist_id, ]

saveRDS(Caribbean_cooc, "results_funbiog/Caribbean_cooc.RDS")

#And now with the Oriental-Indo_Malayan-Papuan-Melanesian
IndoM_eff <- ebd_IndoMalayan |>
  mutate(
    # I don't want here count in X, to convert to NA
    observation_count = as.integer(observation_count),
    # effort_distance_km to 0 for non-travelling counts
    effort_distance_km = if_else(observation_type == "Stationary",
                                 0, effort_distance_km),
    # convert time to decimal hours since midnight
    time_observations_started = time_to_decimal(time_observations_started),
    hour_sampling = round(time_observations_started, 0),
    # split date into year, month, week, and day of year
    year = year(observation_date),
    month = month(observation_date),
    week = week(observation_date),
    day_of_year = yday(observation_date),
    # temporal aggregation - month
    Time.t = case_when(year == 2015 ~ month,
                       year > 2015 ~ month+(12*(year-2015)))) |>
  filter(number_observers <= 10,         #Only list with less than 10 observers
         effort_distance_km <= 5,        #be sure of distance effort
         duration_minutes %in% (0:300), #be sure of duration effort
         !is.na(observation_count))      #remove records without abundance estimation

Checklists_with_more_SppIP <- IndoM_eff %>%
  dplyr::select(checklist_id, scientific_name) %>%
  group_by(checklist_id) %>%
  summarise(N_species = n()) %>%
  filter(N_species >= quantile(N_species, 0.25)) # minimum number of species per sampling - 1st Quartile

IndoMalayan_cooc <- IndoM_eff[IndoM_eff$checklist_id %in% Checklists_with_more_SppIP$checklist_id, ]

saveRDS(IndoMalayan_cooc, "results_funbiog/IndoMalayan_cooc.RDS")

##Spatial Grids ####

# Spatiotemporal Subsampling in diameters of ~11 km (area of ~100 km^2)
# spacing 1 correspond to Characteristic Length Scale, or diameter of spherical cell
dggs_island <- dgconstruct(spacing = 11) 

#add a new variable that identify cell
Caribbean_cooc_cells <- Caribbean_cooc |>
  mutate(cell = dgGEO_to_SEQNUM(dggs_island, #id for cells
                                longitude, latitude)$seqnum) |>
  group_by(cell, scientific_name, Time.t) |>
  # extract the minimum number of individual detected per month per cell of each species
  mutate(Observed.y = round(max(observation_count))) |> 
  ungroup()

# filter cells with more than 25% (1stQ) localities within
cells_1stQ_Ca <- Caribbean_cooc_cells |>
  group_by(cell, locality_id) |>
  count() |>
  group_by(cell) |>
  count() |>
  ungroup() |>
  filter(n >= quantile(n, 0.25))

Caribbean_SS = Caribbean_cooc_cells[Caribbean_cooc_cells$cell %in% cells_1stQ_Ca$cell, ]

saveRDS(Caribbean_SS, "results_funbiog/Caribbean_SS.RDS")

# we can extract the list of species per cell as:

Caribbean_List_Spp <- Caribbean_SS |>
  group_by(cell, scientific_name) |>
  summarise(count=n())

saveRDS(Caribbean_List_Spp, "results_funbiog/Caribbean_List_Spp.RDS")

# Get the number of records in each cell
RichnessCell_Ca   <- Caribbean_SS %>%
  group_by(cell, scientific_name) %>%
  summarise(count=n()) %>%
  group_by(cell) %>%
  summarise(SpRichness=n())

# Get the grid cell boundaries for cells which had quakes
gridCaribbean <- dgcellstogrid(dggs_island, RichnessCell_Ca$cell)

# Update the grid cells' properties to include the number of lists in each cell
gridCaribbean <- merge(gridCaribbean, RichnessCell_Ca, by.x = "seqnum", by.y = "cell")

# Handle cells that cross 180 degrees
wrapped_grid_Caribbean = st_wrap_dateline(gridCaribbean,
                                         options = c("WRAPDATELINE=YES","DATELINEOFFSET=180"), quiet = TRUE)

saveRDS(wrapped_grid_Caribbean, "results_funbiog/wrapped_grid_Caribbean.rds")

# and the IndoMalayan
#add a new variable that identify cell
IndoMalayan_cooc_cells <- IndoMalayan_cooc |>
  mutate(cell = dgGEO_to_SEQNUM(dggs_island, #id for cells
                                longitude, latitude)$seqnum) |>
  group_by(cell, scientific_name, Time.t) |>
  # extract the minimum number of individual detected per month per cell of each species
  mutate(Observed.y = round(max(observation_count))) |> 
  ungroup()

# filter cells with more than 25% (1stQ) localities within
cells_1stQ_IM <- IndoMalayan_cooc_cells |>
  group_by(cell, locality_id) |>
  count() |>
  group_by(cell) |>
  count() |>
  ungroup() |>
  filter(n >= quantile(n, 0.25))

IndoMalayan_SS = IndoMalayan_cooc_cells[IndoMalayan_cooc_cells$cell %in% cells_1stQ_IM$cell, ]

saveRDS(IndoMalayan_SS, "results_funbiog/IndoMalayan_SS.RDS")

# we can extract the list of species per cell as:

IndoMalayan_List_Spp <- IndoMalayan_SS |>
  group_by(cell, scientific_name) |>
  summarise(count=n())

saveRDS(IndoMalayan_List_Spp, "results_funbiog/IndoMalayan_List_Spp.RDS")

# Get the number of records in each cell
RichnessCell_IM   <- IndoMalayan_SS %>%
  group_by(cell, scientific_name) %>%
  summarise(count=n()) %>%
  group_by(cell) %>%
  summarise(SpRichness=n())

# Get the grid cell boundaries for cells which had quakes
gridIndoMalayan <- dgcellstogrid(dggs_island, RichnessCell_IM$cell)

# Update the grid cells' properties to include the number of lists in each cell
gridIndoMalayan <- merge(gridIndoMalayan, RichnessCell_IM, by.x = "seqnum", by.y = "cell")

# Handle cells that cross 180 degrees
wrapped_grid_IndoMalayan = st_wrap_dateline(gridIndoMalayan,
                                         options = c("WRAPDATELINE=YES","DATELINEOFFSET=180"), quiet = TRUE)

saveRDS(wrapped_grid_IndoMalayan, "results_funbiog/wrapped_grid_IndoMalayan.rds")

#End of this code
```

And then, make the scheduler (`island_filtering.sh`) to run this code:

```{r eval = FALSE}
#!/bin/bash
#SBATCH --job-name=Islands_filtering_eBird  # Job name
#SBATCH --mail-type=ALL                    # Mail events
#SBATCH --mail-user=oacevedocharry@ufl.edu # Where to send mail
#SBATCH --ntasks=1                         # Number of processes
#SBATCH --cpus-per-task=16                 # Number of CPU cores per task or process
#SBATCH --mem=128gb                         # Increase per processor memory
#SBATCH --time=30-00:00:00                    # Time limit hrs:min:sec
#SBATCH --output=Islands_filtering_eBird_%j.log             # Standard output and error log
#SBATCH --qos=rcstudents
pwd; hostname; date

echo Working directory is $SLURM_SUBMIT_DIR
cd $SLURM_SUBMIT_DIR

#loading modules for R
module purge
module load R

#Run R script
Rscript 1_a_filtering_eBird.R

date
```

Recall, the scheduler is run through the Terminal connected to the HiPerGator with the command `sbatch island_filtering.sh`. When finished, HiPerGator will send a notification

```{r eval=FALSE}
Job ID                  : 37346924
Cluster                 : hipergator
User                    : oacevedocharry
Group                   : rcstudents
State                   : COMPLETED (exit code 0)
Nodes                   : 1
Cores/Node              : 16
CPU Time Used           : 04:38:11
Wall Time Used          : 04:38:19
CPU Efficiency          : 6.25%
Memory Requested        : 128.00 GB (128.00 GB/node)
Memory Used             : 59.90 GB (3.74 GB/core estimated maximum)
Memory Efficiency       : 46.80%
```

And we have the `.RDS` files in our folder `results_funbiog`.

## Organizing eBird data for stationary distribution population models

At the core of our question is the estimation of a expected log-abundance at stationary distribution to weight the functional traits. We used the Stochastic Gompertz State-Space model (see [Dennis & Ponciano 2014](https://doi.org/10.1890/13-1486.1)). 

The discrete time Stochastic Gompertz State-Space model include the ecological submodel of the population dynamic:

$$
N_t = N_{t-1} e^{a + b \log N_t + E_t};\\
X_t = \log N_t;\\
X_t = a + c X_{t-1} + E_t; \\
E_t \sim \text{Normal}(0, \sigma^2)
$$

And the observation process:
$$
Y_t = X_t + F_t; \\
F_t \sim \text{Normal}(0, \tau^2)
$$. 

\noindent where $N_t$ is the latent (unobserved) abundance at time $t$, $a$ and $b$ are constants describing the maximum intrinsic population growth rate and strength of density dependence (see that $c = b + 1$), respectively. Let $X_t = \log N_t$, and therefore $X_t$ and $Y_t$ are, respectively, the underlying and observed population log-abundance at times $t = 1, 2, ..., q$. The error components include process noise ($E_t$) and observation error ($F_t$), with independent normal distributions with means $0$ and respective variances $\sigma^2$ and $\tau^2$. 

The stochastic differential equation describing the dynamic is:
$$
dN(t) = \theta N(t)[\log \kappa - \log N(t)] dt + \beta N(t) dW(t)
$$
\noindent in which over a tiny time interval, $dt$, an tiny increment of abundance $dN(t)$ is drawn from a normal distribution with a mean $\theta N(t)[\log \kappa - \log N(t)] dt$ and variance $[\beta N(t)]^2 dt$, where $N(t)$ denotes the current abundance, $t$ the current time of population trajectory, whereas $\theta$ and $\kappa$ are constants of equilibrium abundance and speed of equilibrium, respectively. This dynamic is updated as $N(t) + dN(t)$ and $t + dt$. An infinitesimal increment of the Gompertz growth deterministic skeleton (in ordinary differential form $\frac{dn(t)}{dt} = \theta n(t)[\log \kappa - \log n(t)]$) is perturbed by a random noise $dW(t) \sim \text{Normal}(0, dt)$, in which intensity of the noise is scaled by $\beta N(t)$ with $\beta > 0$, denoting the environmental stochasticity. This is a diffusion process with infinitesimal mean $m(n) = \theta n(\log \kappa - \log n)$ and infinitesimal variance $v(n) = \beta^2n^2$. Let $X(t) = g(N(t))$ be a smooth invertible transformation (e.g., $\log N(t)$), the infinitesimal mean $m_{X}$ and variance $v_X$ of $X(t)$ are given by the Itô transformation formulas: $m_X(x) = m(n)g'(n) + (1/2)v(n)g''(n)$, $v_X(x) = v(n)[g'(n)]^2$, with $n = g^{-1}(x)$ and $g'$ and $g''$ representing first and second derivatives. Thus, a given diffusion process might be transformed to a scale discrete process, which exist for the stochastic Gompertz model ([Dennis & Ponciano 2014](https://doi.org/10.1890/13-1486.1)).

The transformation of the stochastic Gompertz model ($X(t) = \log N(t)$) converts the population (log) abundance into the Ornstein-Uhlenbeck diffusion process, with infinitesimal mean $m_X(x) = \theta (\mu - x)$ and variance $v_X(x) = \beta^2$, where $\mu = \log \kappa - \frac{\beta^2}{2 \theta}$. The stochastic differential equation that defines the OU process for $X(t)$ is $dX(t) = \theta[\mu - X(t)]dt + \beta dW(t)$. Therefore, $X(t)$ has a normal transition probability distribution with a mean function $\text{E}[X(t) | X(0) = x_0] = \mu - (\mu - x_0)e^{-\theta t}$, where $x_o = \log n_0$ (see more in [Dennis & Ponciano 2014](https://doi.org/10.1890/13-1486.1)). As time $t$ becomes large, this transition distribution converges to a normal stationary distribution with mean $\text{E}[X(\infty)] = \mu$ and variance $\text{V}[X(\infty)] = \frac{\beta^2}{2 \theta}$. Therefore, on the original scale, the stationary distribution is a lognormal distribution of a cloud of points oscillating around the state, latent abundance, representing a stochastic counterpart to the equilibrium point of deterministic "carrying capacity". The parameters of the ecological process in this continuous form of the stochastic Gompertz model have the equivalent in the discrete time model: $\mu = \frac{a}{1-c}$, $\theta = -\log(c)$, $\beta^2 = \frac{-[2 \sigma^2 \log (c)]}{(1-c^2)}$, while $\tau^2$ is the same for both continuous and discrete models.

The state-space model formulation (OUSS) is, therefore, complemented with the observation submodel. The observations $Y(0), Y(t_1), Y(t_2), ..., Y(t_q)$ are estimates or indices of log-population abundance recorded at times $t_0 = 0, t_1, ..., t_q$, not needed to be equally spaced ([Dennis & Ponciano 2014](https://doi.org/10.1890/13-1486.1)). In our case, it could be the log of the maximum observed counts in a month for each species across each spatiotemporal sampling unit from eBird ($\log O_t = Y_t$). This observed population log-abundance came from potentially 132 months (11 years between 2015-2025), we selected those combination of species/sampling unit of > 12 months to fit the OUSS model.

We can organize the data filtered from eBird as time series per sampling unit for each species.

```{r 2_eBird_data_filter_for_OUSS, eval=FALSE}
# Organizing eBird data for stationary distribution population models

#### Packages ####
library(tidyverse)

#### Data from eBird ####

Caribbean_SS <- readRDS("results_funbiog/Caribbean_SS.RDS") |>
  as.data.frame()

Caribbean_OUSS_Cells <- Caribbean_SS |>
  group_by(scientific_name, cell, Time.t) |> 
  count() |>
  group_by(cell, scientific_name) |>
  count() |>
  filter(n > 12) |> # at least 12 months in 11 years (132 months; between 2015-2025)
  as.data.frame() |>
  rename(n.time = n)

Caribbean_OUSS <- Caribbean_SS |>
  left_join(Caribbean_OUSS_Cells) |>
  filter(!is.na(n.time)) 

Caribbean_OUSS$cell <- as.character(Caribbean_OUSS$cell)

saveRDS(Caribbean_OUSS, "results_funbiog/Caribbean_preOUSS.RDS")

# and now with the Indo Pacific region

IndoPacific_SS <- readRDS("results_funbiog/IndoMalayan_SS.RDS") |>
  as.data.frame()

IndoPacific_OUSS_Cells <- IndoPacific_SS |>
  group_by(scientific_name, cell, Time.t) |> 
  count() |>
  group_by(cell, scientific_name) |>
  count() |>
  filter(n > 12) |> # at least 12 months in 11 years (132 months; between 2015-2025)
  as.data.frame() |>
  rename(n.time = n)

IndoPacific_OUSS <- IndoPacific_SS |>
  left_join(IndoPacific_OUSS_Cells) |>
  filter(!is.na(n.time)) 

IndoPacific_OUSS$cell <- as.character(IndoPacific_OUSS$cell)

saveRDS(IndoPacific_OUSS, "results_funbiog/IndoMalayan_preOUSS.RDS")

# End of this code
```

with it's corresponding scheduler `data_for_ouss.sh`

```{r data_for_ouss, eval=FALSE}
#!/bin/bash
#SBATCH --job-name=eBird_data_for_OUSS  # Job name
#SBATCH --mail-type=ALL                    # Mail events
#SBATCH --mail-user=oacevedocharry@ufl.edu # Where to send mail
#SBATCH --nodes=1                    # Run all processes on a single node
#SBATCH --ntasks=1                         # Number of processes
#SBATCH --cpus-per-task=1                 # Number of CPU cores per task or process
#SBATCH --mem=32gb                         # Increase per processor memory
#SBATCH --time=13-00:00:00                    # Time limit hrs:min:sec
#SBATCH --output=eBird_data_for_OUSS_%j.log             # Standard output and error log
#SBATCH --qos=rcstudents
pwd; hostname; date

echo Working directory is $SLURM_SUBMIT_DIR
cd $SLURM_SUBMIT_DIR

#loading modules for R
module purge
module load R

#Run R script
Rscript 2_eBird_data_filter_for_OUSS.R

date
```

Having the message when it is done:

```{r, eval=FALSE}
Job ID                  : 37506571
Cluster                 : hipergator
User                    : oacevedocharry
Group                   : rcstudents
State                   : COMPLETED (exit code 0)
Cores                   : 1
CPU Time Used           : 00:09:26
Wall Time Used          : 00:09:30
CPU Efficiency          : 99.30%
Memory Requested        : 32.00 GB (32.00 GB/node)
Memory Used             : 29.58 GB (29.58 GB/core estimated maximum)
Memory Efficiency       : 92.44%
```


## Fit eBird data stationary distribution population models

To fit the OUSS model more effectively, we can run it in parallel in the HiPerGator. The `R` codes `3_stationary_abundance_OUSS_Caribbean.R` and `3_stationary_abundance_OUSS_IndoMalayan.R` do this.

### Fitting Caribbean meta archipelago

```{r 3_stationary_abundance_OUSS_Caribbean, eval=FALSE}
# Stationary abundance from eBird in parallel

#### Packages ####
#R functions and datasets to support "Modern Applied Statistics with S", 
#a book from W.N. Venables and B.D. Ripley
library(MASS)
library(tidyverse)

# Wrapped function to estimate OUSS parameters in parallel process. 
 # Data should include: 
	# `scientific_name` for species i
	# `cell` for site or spatial unit m
	# `Time.t` as the time steps for the time series
	# `Observed.y` as the observed abundance`

#### Functions OUSS fit ####
   # Multivariate normal random number generator - State Space models
   randmvn <- function(n, mu.vec, cov.mat){  
   # Save the length of the mean vector of the multivariate normal distribution to sample
   p         <- length(mu.vec);
   # The Cholesky decomposition 
   #(factorization of a real symmetric positive-definite sqr matriz)
   Tau       <- chol(cov.mat, pivot=TRUE);
   # generate normal deviates outside loop
   Zmat      <- matrix(rnorm(n = p  *n, mean = 0 , sd = 1), 
                       nrow = p, ncol = n);
   # empty matrix
   out       <- matrix(0, nrow = p, ncol = n);
   # iterate
   for(i in 1:n){
     Z       <- Zmat[,i];
     out[,i] <- t(Tau) %*% Z + mu.vec
    } 
    return(out)
   }

  # Initial values guess of the parameters
  guess_ouss <- function(yt, tt){
   # Time-vector starting in 0.
   t.i     <- tt - tt[1];
   # Number of time-series transitions
   q       <- length(yt) - 1;
   # length of time-series
   qp1     <- q + 1;
   # time intervals
   t.s     <- t.i[2:qp1] - t.i[1:q];
   # mean of the observations as assumed to arise from stationary distribution
   Ybar    <- mean(yt);
   # Variance of the observations
   Yvar    <- sum((yt - Ybar) * (yt - Ybar)) / q;
   # Initial mu estimate (at stationary distribution)
   mu1     <- Ybar;
   # Kludge an initial value for a based on mean of Y(t+s) given Y(t).
   th1     <- -mean(log(abs((yt[2:qp1] - mu1) / (yt[1:q] - mu1))) / t.s);
   # Moment estimate using stationary distribution
   bsq1    <- 2 * th1 * Yvar / (1 + 2 * th1);
   # Observation error variance, assumed as first guess as betasq=tausq.
   tsq1    <- bsq1; 
   # What to do if initial guesses is three 0's (or NAs)? Assume arbitrary values
   three0s <- sum(c(th1, bsq1, tsq1)) 
   if(three0s == 0 | is.na(three0s)){
     th1   <- 0.5;
     bsq1  <- 0.09;
     tsq1  <- 0.23;}  
   out1    <- c(th1, bsq1, tsq1);
   # What to do if initial guesses are too little? Assume arbitrary values
   if(sum(out1 < 1e-7) >= 1){
     out1  <- c(0.5, 0.09, 0.23)}
     out   <- c(mu1, out1); 
     return(abs(out))
   }

  # Neg-log likelihood
  negloglike_ouss_reml = function(yt, tt, fguess){
   # Constrains parameters theta, beta^2, and tau^2 > 0 
   # speed of equilibration (Eq1 in DP_E)
   theta  <- exp(fguess[2]);
   # variability of process noise
   betasq <- exp(fguess[3]);
   # variability of sampling
   tausq  <- exp(fguess[4]);
   # number of time-series transitions
   q      <- length(yt) - 1;
   # length of time-series
   qp1    <- q + 1;
   # Variance (Eq11 in DP_E)
   Var.inf<- betasq / (2 * theta);
   # time intervals (not used here?)
   t.s    <- tt[2:qp1] - tt[1:q];
   # part of Eq18 in DP_E
   t.cols <- matrix(rep(tt, each = qp1),
                    nrow = qp1,
                    ncol = qp1,
                    byrow = FALSE);
   # (part of Eq18 in DP_E)
   t.rows <- t(t.cols);
   # (part of Eq18 in DP_E)
   abs.diffs     <- abs(t.rows - t.cols); 
   # Covariance of the process (Eq18 in DP_E)
   Sigma.mat     <- Var.inf * exp(-theta * abs.diffs);
   # Create a matrix full of 0s of the length of time series
   Itausq <- matrix(0, qp1, qp1);
   # Repeat the observation error variance guess in the diagonal of the matrix
   diag(Itausq)  <- rep(tausq, qp1);
   # add Covariance with the matrix
   V      <- Sigma.mat + Itausq;
   # Create the differencing matrix **D**
   Dmat   <- cbind(-diag(1, q), matrix(0, q, 1)) + cbind(matrix(0, q, 1), diag(1, q));
   # Variance-covariance matrix **Phi** (Eq20 DP_E)
   Phi.mat<- Dmat %*% V %*% t(Dmat);
   # simple differencing of the observations (W_i? )
   wt     <- yt[2:qp1] - yt[1:q]; 
   # note the signs change because we want here the negative log-likelihood (Eq22*-1)
   neglogl<- (q / 2) * log(2 * pi) + (1 / 2) * log(det(Phi.mat)) + (1/2) * wt %*% ginv(Phi.mat) %*% wt; 
   # What to do if the `neglogl` is not finite? assign a big number of 50000
   if(is.infinite(neglogl) == TRUE){
     return(50000)}else{
       return(neglogl)}
  }

  # OUSS Restricted Maximum Likelihood estimation
  ouss_reml <- function(yt, tt, fguess){ 
   # Time-vector starting in 0.
   t.i           <- tt - tt[1];
   # Number of time-series transitions
   # length of time-series
   q             <- length(yt) - 1;
   qp1           <- q + 1;
   # time intervals
   t.s           <- t.i[2:qp1] - t.i[1:q];
   # initial guesses (all, but negloglike.OU.reml will use only fguess[2:4])
   guess.optim   <- c(fguess[1],
                      log(fguess[2:4]));
   # numerical optimization
   optim.out     <- optim(par = guess.optim,
                          fn = negloglike_ouss_reml,
                          method = "Nelder-Mead",
                          yt = yt,
                          tt = t.i);
   # Restricted maximum likelihood estimates (reml) and lnL.hat
   remls        <- exp(optim.out$par);
   theta.reml   <- remls[2];
   betasq.reml  <- remls[3];
   tausq.reml   <- remls[4]; 
   lnL.hat       <- -optim.out$value[1];  
   # Variance (Eq11 in DP_E)
   Var.inf       <- betasq.reml / (2 * theta.reml)
   # creates an matrix full of 1 dim qp1 x qp1
   vx            <- matrix(1, qp1, qp1);
   # iterate to fill the matrix (couldn't find vx in DP_E!)
   for (t.i in 1:q){
     vx[(t.i + 1):qp1,t.i] = exp(-theta.reml * cumsum(t.s[t.i:q]));
     vx[t.i, (t.i + 1):qp1] = vx[(t.i + 1):qp1, t.i];
   }
   Sigma.mat     <- vx * Var.inf;
   # Create a matrix full of 0s of the length of time series
   Itausq        <- matrix(0, qp1, qp1);
   # Repeat the observation error variance reml in the diagonal of the matrix
   diag(Itausq)  <- rep(tausq.reml, qp1);
   # Variance-covariance matrix (V.hat) evaluated with remls to estimate mu.hat
   V.reml       <- Sigma.mat + Itausq;
   # column vector matrix of ones
   j             <- matrix(1, qp1, 1);
   # Inverse matrix (part of Eq23 in DP_E)
   Vinv          <- ginv(V.reml);
   # reml of mu (mu.hat) with Eq23 in DP_E
   mu.reml      <- (t(j) %*% Vinv %*% yt) / (t(j) %*% Vinv %*% j);
   #AIC
   AIC           <- -2 * lnL.hat + 2 * 4 #where 4 = length(mles)...
   #Results
   out           <- list(remls = c(mu.reml,
                                    theta.reml,
                                    betasq.reml,
                                    tausq.reml),
                         lnLhat = lnL.hat,
                         AIC = AIC)
   return(out)
  }  


# estimate OUSS parameters in a dataset ####
estimate_ouss_parallel <- function(data, n_cores = 6) {
  library(foreach)
  library(doParallel)
  library(dplyr)
  library(MASS)
  
  # Set up parallel backend
  cl <- makeCluster(n_cores)
  on.exit(stopCluster(cl), add = TRUE)
  
  registerDoParallel(cl)
  
  # Create unique species-site combinations
  combos <- data |>
    dplyr::select(scientific_name, 
                  cell,
                  Time.t,
                  Observed.y) |>
    group_by(scientific_name, cell) |>
    dplyr::group_split()
  
  # Message in the log file
  cat(sprintf(
  "[%s] Species-cell combinations: %s\n",
  Sys.time(),
  length(combos)
  ))

  cat(sprintf(
    "[%s] Beginning parallel estimation\n",
    Sys.time()
    ))
  
  # Run parallel estimation
  results_list <- foreach(
    idx = seq_along(combos),
    .packages = c("tidyverse", "MASS"),
    .export = c(
      "combos",
      "randmvn",
      "guess_ouss",
      "negloglike_ouss_reml",
      "ouss_reml"
      )
    ) %dopar% {
      
      timeseries <- combos[[idx]]

      i <- unique(timeseries$scientific_name)
      m <- unique(timeseries$cell)

   # Timeseries construction
    timeseries <- timeseries |>
      group_by(Time.t) |>
      summarise(Observed.y = max(Observed.y),
                .groups = "drop")
    
    if (nrow(timeseries) == 0) return(NULL)
  
  # Time series objects (log abundance + time steps)
    yt <- log(timeseries$Observed.y)
    tt <- timeseries$Time.t

  # Fit OUSS parallel process ####
    fguess <- guess_ouss(yt = yt, tt = tt)
    ouss_im <- ouss_reml(yt = yt, tt = tt, fguess = fguess)
    
    mu_hat <- ouss_im$remls[1]
    theta_hat <- ouss_im$remls[2]
    betasqr_hat <- ouss_im$remls[3]
    tausqrt_hat <- ouss_im$remls[4]
    Var_Xinf <- betasqr_hat / (2 * theta_hat)   # Eq 11 Dennis & Ponciano Ecology 2014
    
    if(Var_Xinf < 2){
      omega_hat <- exp(mu_hat + Var_Xinf / 2) # Eq 49 Dennis et al. Ecol Monogr 2006
    } else {
      omega_hat <- exp(mu_hat)              # Eq 10 Dennis & Ponciano Ecology 2014
    }
    
    data.frame(scientific_name = i, 
               cell = m,
               omega_hat = omega_hat,
               tausqrt_hat = tausqrt_hat,
               mu_hat = mu_hat,
               theta_hat = theta_hat,
               betasqr_hat = betasqr_hat,
               Var_Xinf = Var_Xinf)
  }
  
  cat(sprintf(
    "[%s] Parallel estimation completed\n",
    Sys.time()
    ))
  
  results_df <- bind_rows(results_list)
  
  cat(sprintf(
  "[%s] Rows in output: %s\n",
  Sys.time(),
  nrow(results_df)
  ))
  
  return(results_df)

}

# Fit the model for the Caribbean Meta-Archipelago ####

  
cat(sprintf(
  "[%s] Reading Caribbean dataset\n",
  Sys.time()
))

# Load dataset and name of region
data <- readRDS("results_funbiog/Caribbean_preOUSS.RDS")

cat(sprintf(
  "[%s] Dataset loaded: %s rows\n",
  Sys.time(),
  nrow(data)
))

# Run estimation
  
cat(sprintf(
  "[%s] Starting parallel estimation using 6 cores\n",
  Sys.time()
))

param_df <- estimate_ouss_parallel(data, n_cores = 6)
saveRDS(param_df, paste0("results_funbiog/Caribbean_OUSS_parms_df.RDS"))

cat(sprintf(
  "[%s] Parallel estimation completed and saved\n",
  Sys.time()
))

# Join and save
data <- data |>
  left_join(param_df, by = c("scientific_name", "cell"))
saveRDS(data, paste0("results_funbiog/Caribbean_OUSS_with_parms.RDS"))

# Create cell assembly and save
cell_assembly <- data |>
  dplyr::select(cell, scientific_name, omega_hat) |>
  pivot_wider(names_from = cell,
              values_from = omega_hat,
              values_fill = 0,
              values_fn = mean)

saveRDS(cell_assembly, paste0("results_funbiog/Caribbean_Cell_Assembly_OUSS.RDS"))

# End of this code
```

With the scheduler code `Caribbean_OUSS_fit.sh`:

```{r eval = FALSE}
#!/bin/bash
#SBATCH --job-name=OUSS_Array_Caribbean
#SBATCH --mail-type=ALL
#SBATCH --mail-user=oacevedocharry@ufl.edu
#SBATCH --nodes=1
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=6
#SBATCH --mem=24gb
#SBATCH --time=4-00:00:00
#SBATCH --output=OUSS_Array_Ca_%A_%a.log
#SBATCH --qos=rcstudents

pwd; hostname; date

echo "Working directory is $SLURM_SUBMIT_DIR"
cd $SLURM_SUBMIT_DIR

module purge
module load R

# Run the R script with the appropriate dataset

Rscript 3_stationary_abundance_OUSS_Caribbean.R 

date
```

And the outcome message

```{r, eval=FALSE}
Job ID                  : 37512128
Cluster                 : hipergator
User                    : oacevedocharry
Group                   : rcstudents
State                   : COMPLETED (exit code 0)
Nodes                   : 1
Cores/Node              : 6
CPU Time Used           : 00:08:36
Wall Time Used          : 3-19:22:34
CPU Efficiency          : 0.03%
Memory Requested        : 128.00 GB (128.00 GB/node)
Memory Used             : 21.79 GB (3.63 GB/core estimated maximum)
Memory Efficiency       : 17.03%
```

### Fitting Indo-Pacific meta archipelago

Now with the Indo-Pacific meta archipelago

```{r 3_stationary_abundance_OUSS_IndoMalayan, eval=FALSE}
# Stationary abundance from eBird in parallel

#### Packages ####
#R functions and datasets to support "Modern Applied Statistics with S", 
#a book from W.N. Venables and B.D. Ripley
library(MASS)
library(tidyverse)

# Wrapped function to estimate OUSS parameters in parallel process. 
 # Data should include: 
	# `scientific_name` for species i
	# `cell` for site or spatial unit m
	# `Time.t` as the time steps for the time series
	# `Observed.y` as the observed abundance`

#### Functions OUSS fit ####
   # Multivariate normal random number generator - State Space models
   randmvn <- function(n, mu.vec, cov.mat){  
   # Save the length of the mean vector of the multivariate normal distribution to sample
   p         <- length(mu.vec);
   # The Cholesky decomposition 
   #(factorization of a real symmetric positive-definite sqr matriz)
   Tau       <- chol(cov.mat, pivot=TRUE);
   # generate normal deviates outside loop
   Zmat      <- matrix(rnorm(n = p  *n, mean = 0 , sd = 1), 
                       nrow = p, ncol = n);
   # empty matrix
   out       <- matrix(0, nrow = p, ncol = n);
   # iterate
   for(i in 1:n){
     Z       <- Zmat[,i];
     out[,i] <- t(Tau) %*% Z + mu.vec
    } 
    return(out)
   }

  # Initial values guess of the parameters
  guess_ouss <- function(yt, tt){
   # Time-vector starting in 0.
   t.i     <- tt - tt[1];
   # Number of time-series transitions
   q       <- length(yt) - 1;
   # length of time-series
   qp1     <- q + 1;
   # time intervals
   t.s     <- t.i[2:qp1] - t.i[1:q];
   # mean of the observations as assumed to arise from stationary distribution
   Ybar    <- mean(yt);
   # Variance of the observations
   Yvar    <- sum((yt - Ybar) * (yt - Ybar)) / q;
   # Initial mu estimate (at stationary distribution)
   mu1     <- Ybar;
   # Kludge an initial value for a based on mean of Y(t+s) given Y(t).
   th1     <- -mean(log(abs((yt[2:qp1] - mu1) / (yt[1:q] - mu1))) / t.s);
   # Moment estimate using stationary distribution
   bsq1    <- 2 * th1 * Yvar / (1 + 2 * th1);
   # Observation error variance, assumed as first guess as betasq=tausq.
   tsq1    <- bsq1; 
   # What to do if initial guesses is three 0's (or NAs)? Assume arbitrary values
   three0s <- sum(c(th1, bsq1, tsq1)) 
   if(three0s == 0 | is.na(three0s)){
     th1   <- 0.5;
     bsq1  <- 0.09;
     tsq1  <- 0.23;}  
   out1    <- c(th1, bsq1, tsq1);
   # What to do if initial guesses are too little? Assume arbitrary values
   if(sum(out1 < 1e-7) >= 1){
     out1  <- c(0.5, 0.09, 0.23)}
     out   <- c(mu1, out1); 
     return(abs(out))
   }

  # Neg-log likelihood
  negloglike_ouss_reml = function(yt, tt, fguess){
   # Constrains parameters theta, beta^2, and tau^2 > 0 
   # speed of equilibration (Eq1 in DP_E)
   theta  <- exp(fguess[2]);
   # variability of process noise
   betasq <- exp(fguess[3]);
   # variability of sampling
   tausq  <- exp(fguess[4]);
   # number of time-series transitions
   q      <- length(yt) - 1;
   # length of time-series
   qp1    <- q + 1;
   # Variance (Eq11 in DP_E)
   Var.inf<- betasq / (2 * theta);
   # time intervals (not used here?)
   t.s    <- tt[2:qp1] - tt[1:q];
   # part of Eq18 in DP_E
   t.cols <- matrix(rep(tt, each = qp1),
                    nrow = qp1,
                    ncol = qp1,
                    byrow = FALSE);
   # (part of Eq18 in DP_E)
   t.rows <- t(t.cols);
   # (part of Eq18 in DP_E)
   abs.diffs     <- abs(t.rows - t.cols); 
   # Covariance of the process (Eq18 in DP_E)
   Sigma.mat     <- Var.inf * exp(-theta * abs.diffs);
   # Create a matrix full of 0s of the length of time series
   Itausq <- matrix(0, qp1, qp1);
   # Repeat the observation error variance guess in the diagonal of the matrix
   diag(Itausq)  <- rep(tausq, qp1);
   # add Covariance with the matrix
   V      <- Sigma.mat + Itausq;
   # Create the differencing matrix **D**
   Dmat   <- cbind(-diag(1, q), matrix(0, q, 1)) + cbind(matrix(0, q, 1), diag(1, q));
   # Variance-covariance matrix **Phi** (Eq20 DP_E)
   Phi.mat<- Dmat %*% V %*% t(Dmat);
   # simple differencing of the observations (W_i? )
   wt     <- yt[2:qp1] - yt[1:q]; 
   # note the signs change because we want here the negative log-likelihood (Eq22*-1)
   neglogl<- (q / 2) * log(2 * pi) + (1 / 2) * log(det(Phi.mat)) + (1/2) * wt %*% ginv(Phi.mat) %*% wt; 
   # What to do if the `neglogl` is not finite? assign a big number of 50000
   if(is.infinite(neglogl) == TRUE){
     return(50000)}else{
       return(neglogl)}
  }

  # OUSS Restricted Maximum Likelihood estimation
  ouss_reml <- function(yt, tt, fguess){ 
   # Time-vector starting in 0.
   t.i           <- tt - tt[1];
   # Number of time-series transitions
   # length of time-series
   q             <- length(yt) - 1;
   qp1           <- q + 1;
   # time intervals
   t.s           <- t.i[2:qp1] - t.i[1:q];
   # initial guesses (all, but negloglike.OU.reml will use only fguess[2:4])
   guess.optim   <- c(fguess[1],
                      log(fguess[2:4]));
   # numerical optimization
   optim.out     <- optim(par = guess.optim,
                          fn = negloglike_ouss_reml,
                          method = "Nelder-Mead",
                          yt = yt,
                          tt = t.i);
   # Restricted maximum likelihood estimates (reml) and lnL.hat
   remls        <- exp(optim.out$par);
   theta.reml   <- remls[2];
   betasq.reml  <- remls[3];
   tausq.reml   <- remls[4]; 
   lnL.hat       <- -optim.out$value[1];  
   # Variance (Eq11 in DP_E)
   Var.inf       <- betasq.reml / (2 * theta.reml)
   # creates an matrix full of 1 dim qp1 x qp1
   vx            <- matrix(1, qp1, qp1);
   # iterate to fill the matrix (couldn't find vx in DP_E!)
   for (t.i in 1:q){
     vx[(t.i + 1):qp1,t.i] = exp(-theta.reml * cumsum(t.s[t.i:q]));
     vx[t.i, (t.i + 1):qp1] = vx[(t.i + 1):qp1, t.i];
   }
   Sigma.mat     <- vx * Var.inf;
   # Create a matrix full of 0s of the length of time series
   Itausq        <- matrix(0, qp1, qp1);
   # Repeat the observation error variance reml in the diagonal of the matrix
   diag(Itausq)  <- rep(tausq.reml, qp1);
   # Variance-covariance matrix (V.hat) evaluated with remls to estimate mu.hat
   V.reml       <- Sigma.mat + Itausq;
   # column vector matrix of ones
   j             <- matrix(1, qp1, 1);
   # Inverse matrix (part of Eq23 in DP_E)
   Vinv          <- ginv(V.reml);
   # reml of mu (mu.hat) with Eq23 in DP_E
   mu.reml      <- (t(j) %*% Vinv %*% yt) / (t(j) %*% Vinv %*% j);
   #AIC
   AIC           <- -2 * lnL.hat + 2 * 4 #where 4 = length(mles)...
   #Results
   out           <- list(remls = c(mu.reml,
                                    theta.reml,
                                    betasq.reml,
                                    tausq.reml),
                         lnLhat = lnL.hat,
                         AIC = AIC)
   return(out)
  }  


# estimate OUSS parameters in a dataset ####
estimate_ouss_parallel <- function(data, n_cores = 6) {
  library(foreach)
  library(doParallel)
  library(dplyr)
  library(MASS)
  
  # Set up parallel backend
  cl <- makeCluster(n_cores)
  on.exit(stopCluster(cl), add = TRUE)
  
  registerDoParallel(cl)
  
  # Create unique species-site combinations
  combos <- data |>
    dplyr::select(scientific_name, 
                  cell,
                  Time.t,
                  Observed.y) |>
    group_by(scientific_name, cell) |>
    dplyr::group_split()
  
  # Message in the log file
  cat(sprintf(
  "[%s] Species-cell combinations: %s\n",
  Sys.time(),
  length(combos)
  ))

  cat(sprintf(
    "[%s] Beginning parallel estimation\n",
    Sys.time()
    ))
  
  # Run parallel estimation
  results_list <- foreach(
    idx = seq_along(combos),
    .packages = c("tidyverse", "MASS"),
    .export = c(
      "combos",
      "randmvn",
      "guess_ouss",
      "negloglike_ouss_reml",
      "ouss_reml"
      )
    ) %dopar% {
      
      timeseries <- combos[[idx]]

      i <- unique(timeseries$scientific_name)
      m <- unique(timeseries$cell)

   # Timeseries construction
    timeseries <- timeseries |>
      group_by(Time.t) |>
      summarise(Observed.y = max(Observed.y),
                .groups = "drop")
    
    if (nrow(timeseries) == 0) return(NULL)
  
  # Time series objects (log abundance + time steps)
    yt <- log(timeseries$Observed.y)
    tt <- timeseries$Time.t

  # Fit OUSS parallel process ####
    fguess <- guess_ouss(yt = yt, tt = tt)
    ouss_im <- ouss_reml(yt = yt, tt = tt, fguess = fguess)
    
    mu_hat <- ouss_im$remls[1]
    theta_hat <- ouss_im$remls[2]
    betasqr_hat <- ouss_im$remls[3]
    tausqrt_hat <- ouss_im$remls[4]
    Var_Xinf <- betasqr_hat / (2 * theta_hat)   # Eq 11 Dennis & Ponciano Ecology 2014
    
    if(Var_Xinf < 2){
      omega_hat <- exp(mu_hat + Var_Xinf / 2) # Eq 49 Dennis et al. Ecol Monogr 2006
    } else {
      omega_hat <- exp(mu_hat)              # Eq 10 Dennis & Ponciano Ecology 2014
    }
    
    data.frame(scientific_name = i, 
               cell = m,
               omega_hat = omega_hat,
               tausqrt_hat = tausqrt_hat,
               mu_hat = mu_hat,
               theta_hat = theta_hat,
               betasqr_hat = betasqr_hat,
               Var_Xinf = Var_Xinf)
  }
  
  cat(sprintf(
    "[%s] Parallel estimation completed\n",
    Sys.time()
    ))
  
  results_df <- bind_rows(results_list)
  
  cat(sprintf(
  "[%s] Rows in output: %s\n",
  Sys.time(),
  nrow(results_df)
  ))
  
  return(results_df)

}

# Fit the model for the IndoMalayan Meta-Archipelago ####

  
cat(sprintf(
  "[%s] Reading IndoMalayan dataset\n",
  Sys.time()
))

# Load dataset and name of region
data <- readRDS("results_funbiog/IndoMalayan_preOUSS.RDS")

cat(sprintf(
  "[%s] Dataset loaded: %s rows\n",
  Sys.time(),
  nrow(data)
))

# Run estimation
  
cat(sprintf(
  "[%s] Starting parallel estimation using 6 cores\n",
  Sys.time()
))

param_df <- estimate_ouss_parallel(data, n_cores = 6)
saveRDS(param_df, paste0("results_funbiog/IndoMalayan_OUSS_parms_df.RDS"))

cat(sprintf(
  "[%s] Parallel estimation completed and saved\n",
  Sys.time()
))

# Join and save
data <- data |>
  left_join(param_df, by = c("scientific_name", "cell"))
saveRDS(data, paste0("results_funbiog/IndoMalayan_OUSS_with_parms.RDS"))

# Create cell assembly and save
cell_assembly <- data |>
  dplyr::select(cell, scientific_name, omega_hat) |>
  pivot_wider(names_from = cell,
              values_from = omega_hat,
              values_fill = 0,
              values_fn = mean)

saveRDS(cell_assembly, paste0("results_funbiog/IndoMalayan_Cell_Assembly_OUSS.RDS"))

# End of this code
```

With the scheduler code `IndoMalayan_OUSS_fit.sh`:
```{r eval = FALSE}
#!/bin/bash
#SBATCH --job-name=OUSS_Array_IndoMalayan
#SBATCH --mail-type=ALL
#SBATCH --mail-user=oacevedocharry@ufl.edu
#SBATCH --nodes=1
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=6
#SBATCH --mem=24gb
#SBATCH --time=4-00:00:00
#SBATCH --output=OUSS_Array_IM_%A_%a.log
#SBATCH --qos=rcstudents

pwd; hostname; date

echo "Working directory is $SLURM_SUBMIT_DIR"
cd $SLURM_SUBMIT_DIR

module purge
module load R

# Run the R script with the appropriate dataset

Rscript 3_stationary_abundance_OUSS_IndoMalayan.R 

date
```

And the outcome message

```{r, eval=FALSE}
Job ID                  : 37510169
Cluster                 : hipergator
User                    : oacevedocharry
Group                   : rcstudents
State                   : COMPLETED (exit code 0)
Nodes                   : 1
Cores/Node              : 6
CPU Time Used           : 00:02:19
Wall Time Used          : 14:45:19
CPU Efficiency          : 0.04%
Memory Requested        : 24.00 GB (24.00 GB/node)
Memory Used             : 5.74 GB (979.19 MB/core estimated maximum)
Memory Efficiency       : 23.91%
```

Despite the lower `CPU Efficiency` reported, using less memory ended in an `OUT_OF_MEMORY` state in previous attempts.

### Does the fitted model reflect variance in abundance in the gradient?

Let's see the *Coereba flaveola* estimates across the Caribbean

```{r}
banana <- readRDS("results_funbiog/Species_in_cells_MetaArchipelagos.RDS") |>
  filter(scientific_name == "Coereba flaveola") |>
  mutate(subregion = ifelse(subregion == "Mainland", "Mainland",
                     ifelse(subregion == "Caribbean - Continental islands", "Continental islands",
                     ifelse(subregion == "Caribbean - Greater Antilles", "Greater Antilles",
                     ifelse(subregion == "Caribbean - Kalinago", "Lesser Antilles (Kalinago)",
                     ifelse(subregion == "Caribbean - Lucayan", "Bahamas (Lucayan)",
                     subregion))))),
         subregion = factor(subregion, levels = c("Mainland",
                                                  "Continental islands",
                                                  "Greater Antilles",
                                                  "Lesser Antilles (Kalinago)",
                                                  "Bahamas (Lucayan)")))

ggplot(banana, aes(x = exp(mu_hat), fill = subregion, color = subregion)) +
  geom_density(alpha = 0.2) +
  geom_point(aes(y = 0), shape = "|") +
  facet_wrap(~subregion, ncol = 1, scales = "free_y") +
  scale_color_manual(values = c("#A1A1A1", "#663171", "#aadce0", "#376795", "#ef8a47")) +
  scale_fill_manual(values = c("#A1A1A1", "#663171", "#aadce0", "#376795", "#ef8a47")) +
  labs(x = "Expected abundance mean at stationarity", 
       y = "",
       fill = "",
       color = "") +
  theme_classic() +
  theme(legend.position = "none")

# map
world1 <- sf::st_as_sf(maps::map(database = 'world', plot = FALSE, fill = TRUE))

ggplot() +
  geom_sf(data = world1) +
  geom_point(data = banana |>
               group_by(cell) |> 
               sample_n(size = 1), 
             aes(x = longitude, y = latitude, 
                 color = subregion, size = exp(mu_hat)), 
             shape = 21, stroke = 0.5, fill = NA, alpha = 0.8) +
  scale_color_manual(values = c("#A1A1A1", "#663171", "#aadce0", "#376795", "#ef8a47")) +
  scale_size_continuous(range = c(0.05, 5)) +
  coord_sf(xlim = c(-91, -59), 
           ylim =  c(8, 27),
           expand = T) +
  labs(color = "", size = expression(exp^hat(mu))) +
  theme_bw() +
  theme(legend.position = "bottom") +
  guides(color = guide_legend(nrow = 3, byrow = TRUE), 
         size = guide_legend(direction = "vertical"))

```

## Functional trait space

### Spatial assignation for the filtered and fitted records

Data records from eBird (after filtering and fitting the population dynamics models)

```{r, eval = FALSE}
Caribbean_Spp_sf <- readRDS("results_funbiog/Caribbean_OUSS_with_parms.RDS") |>
  group_by(scientific_name, cell) |>
  summarise(latitude = mean(latitude, na.rm = TRUE),
            longitude = mean(longitude, na.rm = TRUE),
            mu_hat = mean(mu_hat, na.rm = TRUE),
            omega_hat = mean(omega_hat, na.rm = TRUE)) |>
  as.data.frame() |>
  mutate(Meta.Archipelago = "Caribbean") |>
  st_as_sf(coords = c("longitude", "latitude"),
           crs = 4326)

IndoPacific_Spp_sf <- readRDS("results_funbiog/IndoMalayan_OUSS_with_parms.RDS") |> 
  group_by(scientific_name, cell) |>
  summarise(latitude = mean(latitude, na.rm = TRUE),
            longitude = mean(longitude, na.rm = TRUE),
            mu_hat = mean(mu_hat, na.rm = TRUE),
            omega_hat = mean(omega_hat, na.rm = TRUE)) |>
  as.data.frame() |>
  mutate(Meta.Archipelago = "Indo.Pacific") |>
  st_as_sf(coords = c("longitude", "latitude"),
           crs = 4326)

islands_spp_cells <- rbind(Caribbean_Spp_sf, IndoPacific_Spp_sf)

# recover latitude and longitude
islands_spp_cells$longitude <- st_coordinates(islands_spp_cells)[, 1]
islands_spp_cells$latitude <- st_coordinates(islands_spp_cells)[, 2]

# save backup
saveRDS(islands_spp_cells, "results_funbiog/islands_spp_cells.RDS")
```

Load polygons of the meta-archipelagos and 

```{r eval = FALSE}
islands_spp_cells <- readRDS("results_funbiog/islands_spp_cells.RDS")

# Mainland in america polygon
mainland_sf <- st_read("Coastline_Islands/Mainland_coastline.shp") |>
  st_transform(crs = 4326) |>
  mutate(subregion = "Mainland")

# Caribbean ####
# Continental Islands polygon
cont_isln_Car <- st_read("Coastline_Islands/ContinentalIslands.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Caribbean - Continental islands")
# Greater Antilles polygon
gr_antilles <- st_read("Coastline_Islands/GreaterAntilles.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Caribbean - Greater Antilles")
# Kalinago or Lesser Antilles polygon
kalinago <- st_read("Coastline_Islands/Kalinago.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Caribbean - Kalinago")
# Lucayan or Bahamas polygon
lucayan <- st_read("Coastline_Islands/Lucayan.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Caribbean - Lucayan")

# Oriental-Indo-Malayan ####
# Andaman & Nicobar polygon
andaman <- st_read("Coastline_Islands/AndamanNicobar.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Oriental-Indo-Malayan - Andaman & Nicobar")
# Continental Islands - Oriental polygon
cont_isln_OrInd <- st_read("Coastline_Islands/ContinentalIslandsOriental.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Oriental-Indo-Malayan - Continental islands")
# Philippines polygon
philippines <- st_read("Coastline_Islands/Philippines.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Oriental-Indo-Malayan - Philippines")
# Sunda Islands polygon
sunda <- st_read("Coastline_Islands/SundaIslands.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Oriental-Indo-Malayan - Sunda islands")
# Wallacea polygon
wallacea <- st_read("Coastline_Islands/Wallacea.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Oriental-Indo-Malayan - Wallacea")

# Papuan-Melanesian ####
# Bismarck polygon
bismarcks <- st_read("Coastline_Islands/Biskmark.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Papuan-Melanesian - Bismarcks")
# New Guinea and Australian continental islands polygon
papua <- st_read("Coastline_Islands/NewGuinea_AU_ContIslands.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Papuan-Melanesian - Papua")
# Solomons polygon
solomons <- st_read("Coastline_Islands/Solomon.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Papuan-Melanesian - Solomons")
# Vanuatu polygon
vanuatu <- st_read("Coastline_Islands/Vanuatu.shp") |>
  st_transform(crs = 4326) |>
  st_make_valid() |>
  mutate(subregion = "Papuan-Melanesian - Vanuatu")

# Combine the sf objects for the islands
islands_sf <- rbind(cont_isln_Car, gr_antilles, kalinago, lucayan, 
                     cont_isln_OrInd, andaman, philippines, sunda, wallacea,  
                     papua, bismarcks, solomons, vanuatu)

# Save backup
saveRDS(islands_sf, "results_funbiog/islands_sf.RDS")

# Spatial join to filter points withIN the mainland ####
mainland_cells <- st_join(islands_spp_cells, 
                           mainland_sf, 
                           join = st_within)

#Filter only points within the mainland
mainland_cells <- mainland_cells[!is.na(mainland_cells$subregion), ]
# 214789 observations

#recover latitude and longitude
mainland_cells$longitude <- st_coordinates(mainland_cells)[, 1]
mainland_cells$latitude <- st_coordinates(mainland_cells)[, 2]

#bring back the df structure
spp_cell_mainland <- mainland_cells |>
  st_drop_geometry() |>
  as.data.frame()

# Spatial join to filter points withOUT the mainland ####
island_cells <- st_join(islands_spp_cells, 
                          islands_sf, 
                          join = st_within)

#Filter only points withOUT the mainland
island_cells <- island_cells[!is.na(island_cells$subregion), ]
# 52020 (this removes islands out of our regions - Micronesia)

#recover latitude and longitude
island_cells$longitude <- st_coordinates(island_cells)[, 1]
island_cells$latitude <- st_coordinates(island_cells)[, 2]

#bring back the df structure
spp_cell_islands <- island_cells |>
  st_drop_geometry() |>
  as.data.frame()

# Combine df with subregion #### 
spp_cell_df <- spp_cell_mainland |> 
  full_join(spp_cell_islands)

# Save data for PCAs figures ####

saveRDS(spp_cell_df, "results_funbiog/Species_in_cells_MetaArchipelagos.RDS")
```

### Functional traits imputation

```{r, eval=FALSE}
spp_cell_df <- readRDS("results_funbiog/Species_in_cells_MetaArchipelagos.RDS")

functraits <- readRDS("FunctionalTraits/eBird_func_taxa.RDS") |>
  filter(scientific_name %in% spp_cell_df$scientific_name) 

birdbase_long <- read_csv("FunctionalTraits/BirdBase.csv") |> 
  # only sedentary or resident species NOT from the Sea, Wetland, Riparian, or Coastal primary habitat
  filter(`Mobility - Sedentary or Resident` == 1,
         `Habitat - Primary Habitat` != "Sea", 
         `Habitat - Primary Habitat` != "Wetland", 
         `Habitat - Primary Habitat` != "Riparian",
         `Habitat - Primary Habitat` != "Coastal") |>
  # adjust traits for functional space estimation
  mutate(ESI = log10(100 / (`Habitat - Habitat Breadth` * (`Diet - Diet Breadth` + 0.1))), 
         `Body mass` = log(`Body Mass - Average`), 
         `Clutch size` = ((`Reproductive Behavior - Max Clutch Size` + `Reproductive Behavior - Min Clutch Size`) / 2),
         `Number of broods` = ((`Reproductive Behavior - Max Number of Broods` + `Reproductive Behavior - Min Number of Broods`) / 2),
         `Nest location breadth` = str_count(`Reproductive Behavior - Nest Substrate`, "[A-Za-z]"), 
         `Nest type breadth` = ifelse(`Reproductive Behavior - Nest Type` == "", 0, str_count(`Reproductive Behavior - Nest Type`, ",") + 1)) |>
  # in long format
  pivot_longer(    
    cols = c(`Latin (BirdLife_IOC_Clements_AviList)`,
             `HBW/BirdLife International (v9.1)`,
             `IOC World Bird List (v15.1)`,
             `eBird/Clements (V2024b)`,
             `AviList v1 2025`),    
    names_to = "taxonomy_source_birdbase",    
    values_to = "species_name"  
    ) |>
  filter(!is.na(species_name)) |>
  dplyr::select(species_name, `Habitat - Primary Habitat`, `Diet - Vertebrate`) |>
  mutate(`Diet - Vertebrate` = as.numeric(ifelse(`Diet - Vertebrate` == "T", 0.1,
                                                 `Diet - Vertebrate`))) |>
  unique() |> 
  as.data.frame()

functraits <- functraits |>
  filter(scientific_name %in% birdbase_long$species_name) |>
  left_join(birdbase_long |> rename(scientific_name = species_name)) |>
  as.data.frame()

summary(functraits) # there are some NAs in many traits

# Phylogenetic tree
birds_phylo <- ape::read.nexus("summary_dated_clements.nex")
length(birds_phylo$tip.label)

# column following the same format
functraits$phylo_name <- gsub(" ", "_", functraits$scientific_name)

# trees work with the rowname as the taxa
rownames(functraits) <- functraits$phylo_name

# to see the traits to impute
names(functraits)

# impute in NAs from the phylogenetic information
imputed_traits <- impute(functraits[,c(2:18,20)], phylo = birds_phylo)

# Save the imputed traits
functraits_global <- imputed_traits$imputed

functraits_global$scientific_name <- functraits$scientific_name

saveRDS(functraits_global, "FunctionalTraits/birds_functraits_global_imputed.RDS")
```

### Functional traits to Community Weighted Mean

```{r}
# Phylogenetic information
birds_phylo <- ape::read.nexus("summary_dated_clements.nex")

# global functional traits (after imputation)
functraits_global <- readRDS("FunctionalTraits/birds_functraits_global_imputed.RDS") 

spp_cell_df <- readRDS("results_funbiog/Species_in_cells_MetaArchipelagos.RDS") |>
  filter(scientific_name %in% functraits_global$scientific_name)

spp_cell_df_func <- spp_cell_df |>
  left_join(functraits_global) |>
  group_by(cell, scientific_name) |>
  # mean abundance per species per cell
  mutate(mu_hat = mean(mu_hat, na.rm = TRUE),
         omega_hat = mean(omega_hat, na.rm = TRUE)) |>
  as.data.frame() 

names(spp_cell_df_func)
```

For every trait $j$ and sampling unit (cell) $m$, we calculated the cell-level trait means as community-weighted mean:

$$
\textit{CWM}_{j,m} = \sum_s^{S_m} r_{s,m} t_{s,j}
$$

\noindent where $S_m$ is the number of species per cell $m$, $r_{s,m}$ is the relative abundance of species $s$ in plot $m$ (computed from the sum of estimated log-abundance mean at stationarity $\mu$), and $t_{s,j}$ is the mean value of species $s$ for trait $j$. This is achieved with the function `weighted.mean()` in `R`. But we can also compute the within-cell trait variance with the community-weighted variance as:

$$
\textit{CWV}_{j,m} = \sum_s^{S_m} r_{s,m} (t_{s,j} - CWM_{j,m})^2
$$

We generated the function:

```{r}
# function to compute the community weighted variance
cwv <- function(x, wt, CWM){
  sum(wt*(x-CWM)^2)
}
```

And compute the `CWM` and `CWV`

```{r}
# Calculating the CWM and CWV

islands_cwm_mu_summary <- spp_cell_df_func |>
  group_by(cell, Meta.Archipelago, subregion) |>
  mutate(total_mu_hat = sum(mu_hat)) |> 
  ungroup() |>
  mutate(rel_mu = mu_hat/total_mu_hat) |> 
  group_by(cell, Meta.Archipelago, subregion) |> 
  summarise(spp_richness = n(),
            pred_richness = sum(`Diet - Vertebrate` > 5, na.rm = TRUE),
          # CWM
            `body mass` = weighted.mean(`log (Body mass)`, rel_mu, na.rm = T),
            `hand wing index` = weighted.mean(`Hand Wing Index`, rel_mu, na.rm = T),
            `tarsus length` = weighted.mean(`Tarsus length`, rel_mu, na.rm = T),
            `wing length` = weighted.mean(`Wing length`, rel_mu, na.rm = T),
            `beak length nar` = weighted.mean(`Beak length (nares)`, rel_mu, na.rm = T), 
          `beak length cul` = weighted.mean(`Beak length (culmen)`, rel_mu, na.rm = T), 
            `beak width` = weighted.mean(`Beak width`, rel_mu, na.rm = T),
            `beak depth` = weighted.mean(`Beak depth`, rel_mu, na.rm = T), 
            `diet breadth` = weighted.mean(`Diet breadth`, rel_mu, na.rm = T), 
            `habitat breadth` = weighted.mean(`Habitat breadth`, rel_mu, na.rm = T),
            `eco speci index` = weighted.mean(`EcoloSpeciIndex`, rel_mu, na.rm = T),
            `verticality` = weighted.mean(`Verticality index`, rel_mu, na.rm = T),
            `elevation range` = weighted.mean(`Elevational range`, rel_mu, na.rm = T),
            `clutch size` = weighted.mean(`Clutch size`, rel_mu, na.rm = T),
            `n broods` = weighted.mean(`Number of broods`, rel_mu, na.rm = T),
            `nest locat breadth` = weighted.mean(`Nest location breadth`, rel_mu, na.rm = T),
            `nest type breadth` = weighted.mean(`Nest type breadth`, rel_mu, na.rm = T),
          # CWV  
            `mass cwv` = cwv(`log (Body mass)`, rel_mu, `body mass`),
            `hwi cwv` = cwv(`Hand Wing Index`, rel_mu, `hand wing index`),
            `tarsus l cwv` = cwv(`Tarsus length`, rel_mu, `tarsus length`),
            `wing l cwv` = cwv(`Wing length`, rel_mu, `wing length`),
            `beak l-n cwv ` = cwv(`Beak length (nares)`, rel_mu, `beak length nar`), 
          `beak l-c cwv` = cwv(`Beak length (culmen)`, rel_mu, `beak length cul`), 
            `beak w cwv` = cwv(`Beak width`, rel_mu, `beak width`),
             `beak d cwv` = cwv(`Beak depth`, rel_mu, `beak depth`), 
            `diet b cwv` = cwv(`Diet breadth`, rel_mu, `diet breadth`), 
            `habitat b cwv` = cwv(`Habitat breadth`, rel_mu, `habitat breadth`),
            `esi cwv` = cwv(`EcoloSpeciIndex`, rel_mu, `eco speci index`),
            `verticality cwv` = cwv(`Verticality index`, rel_mu, `verticality`),
            `elevation r cwv` = cwv(`Elevational range`, rel_mu, `elevation range`),
            `clutch s cwv` = cwv(`Clutch size`, rel_mu, `clutch size`),
            `n broods cwv` = cwv(`Number of broods`, rel_mu, `n broods`),
            `nest l b cwv` = cwv(`Nest location breadth`, rel_mu, `nest locat breadth`),
            `nest t b cwv` = cwv(`Nest type breadth`, rel_mu, `nest type breadth`)
            ) |>
  as.data.frame()
```

Group for figure

```{r}
islands_cwm_mu <- islands_cwm_mu_summary |>
  mutate(subregion = ifelse(subregion == "Mainland", "Mainland",
                     ifelse(subregion == "Caribbean - Continental islands", "Continental islands",
                     ifelse(subregion == "Caribbean - Greater Antilles", "Greater Antilles",
                     ifelse(subregion == "Caribbean - Kalinago", "Lesser Antilles (Kalinago)",
                     ifelse(subregion == "Caribbean - Lucayan", "Bahamas (Lucayan)",
                     ifelse(subregion == "Oriental-Indo-Malayan - Andaman & Nicobar", "Andaman & Nicobar",
                     ifelse(subregion == "Oriental-Indo-Malayan - Continental islands", "Continental islands",
                     ifelse(subregion == "Oriental-Indo-Malayan - Philippines", "Philippines",
                     ifelse(subregion == "Oriental-Indo-Malayan - Sunda islands", "Sunda islands",
                     ifelse(subregion == "Oriental-Indo-Malayan - Wallacea", "Wallacea",
                     ifelse(subregion == "Papuan-Melanesian - Bismarcks", "Bismarcks",
                     ifelse(subregion == "Papuan-Melanesian - Papua", "Papua",
                     ifelse(subregion == "Papuan-Melanesian - Solomons", "Solomons",
                     ifelse(subregion == "Papuan-Melanesian - Vanuatu", "Vanuatu",
                     subregion)))))))))))))),
         region = ifelse(Meta.Archipelago == "Indo.Pacific", "Indo Pacific",
                                   "Caribbean"),
         fig_group = ifelse(subregion == "Mainland", "Mainland",
                     ifelse(subregion == "Continental islands", "Islands",
                     ifelse(subregion == "Greater Antilles", "Islands",
                     ifelse(subregion == "Lesser Antilles (Kalinago)", "Islands",
                     ifelse(subregion == "Bahamas (Lucayan)", "Islands",
                     ifelse(subregion == "Andaman & Nicobar", "Islands",
                     ifelse(subregion == "Philippines", "Islands",
                     ifelse(subregion == "Sunda islands", "Islands",
                     ifelse(subregion == "Wallacea", "Islands",
                     ifelse(subregion == "Bismarcks", "Islands",
                     ifelse(subregion == "Papua", "Islands",
                     ifelse(subregion == "Solomons", "Islands",
                     ifelse(subregion == "Vanuatu", "Islands",
                     subregion))))))))))))))

names(islands_cwm_mu)

corrplot::corrplot(round(cor(scale(log10(islands_cwm_mu[,c(6:22)]))),2), 
         type="upper", order="hclust", 
         tl.col="black", tl.srt=45)

corrplot::corrplot(round(cor(scale((islands_cwm_mu[,c(23:39)]))),2), 
         type="upper", order="hclust", 
         tl.col="black", tl.srt=45)

# Save data for PCAs figure!! ####
saveRDS(islands_cwm_mu, "results_funbiog/Islands_CWM_mu.RDS")
```

### PCA of the CWM and CWV

```{r}
islands_cwm_mu <- readRDS("results_funbiog/Islands_CWM_mu.RDS")

caribbean_cwm <- islands_cwm_mu |>
  filter(Meta.Archipelago %in% "Caribbean") |>
  # bring back the mean value of longitude for each cell
  left_join(spp_cell_df |> 
              dplyr::select(cell,longitude, latitude) |> 
              group_by(cell) |> 
              summarize(longitude = mean(longitude),
                        latitude = mean(latitude))) |>
  filter(latitude < 24 | longitude > -79.5, 
         spp_richness >= 15) # 1st Q of species richness in the Caribbean

names(caribbean_cwm)

caribbean_cwm[,c(3, 6:22)] |>
  pivot_longer(cols = !c(subregion), names_to = "Trait",values_to = "CWM") |>
  ggplot(aes(x = scale(log10(CWM)))) + geom_histogram() +facet_wrap(~Trait, scales = "free")+theme_classic()

funspaceDim(scale(log10(caribbean_cwm[,c(6:22)]))) # 4 dimensions

carib.pca.cwm <- prcomp(scale(log10(caribbean_cwm[,c(6:22)])))
carib.pca.cwm.values <- data.frame(cell = caribbean_cwm$cell,
                               Meta_Archipelago = caribbean_cwm$Meta.Archipelago,
                               region = caribbean_cwm$region,
                               subregion = caribbean_cwm$subregion,
                               fig_group = caribbean_cwm$fig_group,
                               spp_richness = caribbean_cwm$spp_richness,
                               pred_richness = caribbean_cwm$pred_richness,
                               carib.pca.cwm$x)

carib.pca.cwm.loadings <- data.frame(Variables = rownames(carib.pca.cwm$rotation), 
                                     carib.pca.cwm$rotation)

aload.car <- abs(carib.pca.cwm$rotation)
sweep(aload.car, 2, colSums(aload.car), "/")

table(carib.pca.cwm.values$subregion)

carib.pca.cwm.values$subregion <- factor(carib.pca.cwm.values$subregion,
                                          levels = c("Mainland",
                                                     "Continental islands",
                                                     "Lesser Antilles (Kalinago)",
                                                     "Greater Antilles",
                                                     "Bahamas (Lucayan)"))

summary(carib.pca.cwm)

saveRDS(carib.pca.cwm.values, "results_funbiog/carib.pca.cwm.values.RDS")
saveRDS(carib.pca.cwm.loadings, "results_funbiog/carib.pca.cwm.loadings.RDS")
```

```{r}
library(plotly)

# Define your palette
subregion_colors <- c(
  "Mainland"            = "#A1A1A1",
  "Continental islands" = "#663171",
  "Lesser Antilles (Kalinago)"     = "#aadce0",
  "Greater Antilles"    = "#376795",
  "Bahamas (Lucayan)"             = "#ef8a47"
)

# Scale richness to a reasonable marker size range
size_scale <- scales::rescale(carib.pca.cwm.values$spp_richness, to = c(4, 18))

# Base scatter plot with relative size
p <- plot_ly(
  data = carib.pca.cwm.values,
  x = ~PC1, y = ~PC2, z = ~PC3,
  type = "scatter3d", mode = "markers",
  color = ~subregion,
  colors = subregion_colors,
  marker = list(
    size = size_scale,   # relative size by richness
    opacity = 0.5
  ),
  text = ~paste("Richness:", spp_richness) # hover info
)

# Add loading vectors for top contributors
carib.pca.cwm.loadings$contrib <- rowSums(carib.pca.cwm.loadings[, c("PC1","PC2","PC3")]^2)
top_vars <- carib.pca.cwm.loadings[order(-carib.pca.cwm.loadings$contrib), ][1:10, ]

for (i in seq_len(nrow(top_vars))) {
  v <- top_vars[i, ]
  
  # Arrow (line only)
  p <- add_trace(
    p,
    x = c(0, v$PC1 * 10),
    y = c(0, v$PC2 * 10),
    z = c(0, v$PC3 * 10),
    type = "scatter3d", mode = "lines",
    line = list(color = "black", width = 4),
    showlegend = FALSE,
    inherit = FALSE
  )
  
  # Label (only at arrow tip)
  p <- add_trace(
    p,
    x = v$PC1 * 10,
    y = v$PC2 * 10,
    z = v$PC3 * 10,
    type = "scatter3d", mode = "text",
    text = v$Variables,
    textposition = "top middle",
    showlegend = FALSE,
    inherit = FALSE
  )
}

p
```

```{r}
table(islands_cwm_mu$Meta.Archipelago)

orinma_cwm <- islands_cwm_mu |> 
  filter(Meta.Archipelago == "Indo.Pacific") |>
  # bring back the mean value of longitude for each cell
  left_join(spp_cell_df |> 
              dplyr::select(cell,longitude, latitude) |> 
              group_by(cell) |> 
              summarize(longitude = mean(longitude),
                        latitude = mean(latitude))) |>
  filter(spp_richness >= 7, # 1st Q of Species richness 
         subregion != "Vanuatu", # 9 cells
         subregion != "Solomons", # 2 cells
         subregion != "Bismarcks", # 2 cells
         # removing Australia (lose of ~6 Papua)
         latitude > -10.7)

names(orinma_cwm)

funspaceDim(scale(log10(orinma_cwm[,c(6:22)]))) # 3 dimensions

orinma.pca.cwm <- prcomp(scale(log10(orinma_cwm[,c(6:22)])))

orinma.pca.cwm.values <- data.frame(cell = orinma_cwm$cell,
                                Meta_Archipelago = orinma_cwm$Meta.Archipelago,
                                region = orinma_cwm$region,
                                subregion = orinma_cwm$subregion,
                                fig_group = orinma_cwm$fig_group,
                                spp_richness = orinma_cwm$spp_richness,
                                pred_richness = orinma_cwm$pred_richness,
                                orinma.pca.cwm$x)

orinma.pca.cwm.loadings <- data.frame(Variables = rownames(orinma.pca.cwm$rotation), orinma.pca.cwm$rotation)

aload <- abs(orinma.pca.cwm$rotation)
sweep(aload, 2, colSums(aload), "/")

table(orinma.pca.cwm.values$subregion)

orinma.pca.cwm.values$subregion <- factor(orinma.pca.cwm.values$subregion,
                                          levels = c("Mainland",
                                                     "Continental islands",
                                                     "Andaman & Nicobar",
                                                     "Sunda islands",
                                                     "Philippines",
                                                     "Wallacea",
                                                     "Papua"),
                                          labels = c("Mainland",
                                                     "Continental islands",
                                                     "Andaman & Nicobar",
                                                     "Sunda islands",
                                                     "Philippines",
                                                     "Wallacea",
                                                     "New Guinea"))

summary(orinma.pca.cwm)

saveRDS(orinma.pca.cwm.values, "results_funbiog/orinma.pca.cwm.values.RDS")
saveRDS(orinma.pca.cwm.loadings, "results_funbiog/orinma.pca.cwm.loadings.RDS")
```

```{r}
library(plotly)

# Define your palette
subregion_colorsI <- c(
  "Mainland"            = "#A1A1A1",
  "Continental islands" = "#663171",
  "Andaman & Nicobar" = "#BA3841",
  "Sunda islands" = "#DF5C2D",
  "Philippines" = "#E6824F",
  "Wallacea" = "#B7917F",
  "New Guinea" = "#0C7156"
)

# Scale richness to a reasonable marker size range
size_scaleI <- scales::rescale(orinma.pca.cwm.values$spp_richness, to = c(4, 18))

# Base scatter plot with relative size
pI <- plot_ly(
  data = orinma.pca.cwm.values,
  x = ~PC1, y = ~PC2, z = ~PC3,
  type = "scatter3d", mode = "markers",
  color = ~subregion,
  colors = subregion_colorsI,
  marker = list(
    size = size_scale,   # relative size by richness
    opacity = 0.6
  ),
  text = ~paste("Richness:", spp_richness) # hover info
)

# Add loading vectors for top contributors
orinma.pca.cwm.loadings$contrib <- rowSums(orinma.pca.cwm.loadings[, c("PC1","PC2","PC3")]^2)
top_varsI <- orinma.pca.cwm.loadings[order(-orinma.pca.cwm.loadings$contrib), ][1:10, ]

for (i in seq_len(nrow(top_varsI))) {
  v <- top_varsI[i, ]
  
  # Arrow (line only)
  pI <- add_trace(
    pI,
    x = c(0, v$PC1 * 10),
    y = c(0, v$PC2 * 10),
    z = c(0, v$PC3 * 10),
    type = "scatter3d", mode = "lines",
    line = list(color = "black", width = 4),
    showlegend = FALSE,
    inherit = FALSE
  )
  
  # Label (only at arrow tip)
  pI <- add_trace(
    pI,
    x = v$PC1 * 10,
    y = v$PC2 * 10,
    z = v$PC3 * 10,
    type = "scatter3d", mode = "text",
    text = v$Variables,
    textposition = "top middle",
    showlegend = FALSE,
    inherit = FALSE
  )
}

pI
```

## Analyses of Expansion or Shift

Consider that species richness, $S_m$, is partially represented by potential predators ($P_m$) and potential competitors ($C_m$):
$$
S_m = C_m + P_m
$$

Therefore, we can compare the marginal effect of these two components of species richness in the context of competition ($\sim C_m$) and predation ($\sim P_m$) release.

Bring the pca values. 

```{r}
carib.pca.cwm.values <- readRDS("results_funbiog/carib.pca.cwm.values.RDS")
carib.pca.cwm.loadings <- readRDS("results_funbiog/carib.pca.cwm.loadings.RDS")
orinma.pca.cwm.values <- readRDS("results_funbiog/orinma.pca.cwm.values.RDS")
orinma.pca.cwm.loadings <- readRDS("results_funbiog/orinma.pca.cwm.loadings.RDS")
```

Euclidean distance of ordination for CWM will provide "expansion" or "retraction" across regions (and effect of non-predator richness as proxy for competition and predator richness as proxy for predation).

### Caribbean

```{r}
names(carib.pca.cwm.values)

carib.bd <- betadisper(dist(cbind(carib.pca.cwm.values[,8:11])), #PC1:PC4
                       carib.pca.cwm.values$subregion, type = "centroid")
TukeyHSD(carib.bd) # no difference in distance

carib.disp_df <- data.frame(distances = carib.bd$distances,
                            S_m = carib.pca.cwm.values$spp_richness,
                             C_m = (carib.pca.cwm.values$spp_richness - carib.pca.cwm.values$pred_richness),
                             P_m = carib.pca.cwm.values$pred_richness,
                             subregion = carib.pca.cwm.values$subregion)

# compare models
carib.mod.Null <- glm(distances ~ 1, 
                      data = carib.disp_df)
carib.mod.Subr <- glm(distances ~ subregion, 
                    data = carib.disp_df)
carib.mod.Sm <- glm(distances ~ C_m, 
                    data = carib.disp_df)
carib.mod.Pm <- glm(distances ~ P_m, 
                    data = carib.disp_df)
carib.mod.Sm.Pm <- glm(distances ~ C_m + P_m, 
                       data = carib.disp_df)
carib.mod.Sm.Subr.Pm <- glm(distances ~ C_m*subregion + P_m, 
                         data = carib.disp_df)
carib.mod.Pm.Subr.Sm <- glm(distances ~ P_m*subregion + C_m, 
                    data = carib.disp_df)
AICcmodavg::aictab(cand.set = list(carib.mod.Null, 
                                   carib.mod.Subr,
                                   carib.mod.Sm, 
                                   carib.mod.Pm,
                                   carib.mod.Sm.Pm,
                                   carib.mod.Sm.Subr.Pm,
                                   carib.mod.Pm.Subr.Sm),
                   modnames = c("Null", 
                                "Subregion",
                                "Cm","Pm",
                                "Cm_Pm",
                                "Cm_Subregion_Pm","Pm_Subregion_Cm"))

summary(carib.mod.Sm.Subr.Pm)

carib.mod.Sm.Subr.eff <- emtrends(carib.mod.Sm.Subr.Pm, ~ subregion, var = "C_m") |>
  as.data.frame()

carib.mod.Sm.Subr.eff |>
  ggplot(aes(x = subregion, color = subregion)) +
  geom_hline(yintercept = 0, lty = 2) +
  geom_pointrange(aes(y = C_m.trend, ymin = lower.CL, ymax = upper.CL), 
                  alpha = 0.8, size = 1) +
  scale_color_manual(values = c("Mainland"            = "#A1A1A1",
  "Continental islands" = "#663171",
  "Lesser Antilles (Kalinago)"     = "#aadce0",
  "Greater Antilles"    = "#376795",
  "Bahamas (Lucayan)"             = "#ef8a47")) +
  labs(y = "EMMs of `competitors` effect on Euclidean distance", 
       x = "Subregion", title = "Caribbean") +
  coord_flip() +
  theme_classic() +
  theme(legend.position = "none")

ggplot(carib.disp_df, 
       aes(x = C_m, 
           y = distances, 
           color = subregion, 
           fill = subregion)) + 
  geom_point(alpha = 0.2) +
  geom_smooth(method = "lm", alpha = 0.2, lwd = 1) +
#  scale_x_log10() +
  scale_color_manual(values = c("Mainland" = "#A1A1A1",
                                  "Continental islands" = "#663171",
                                  "Lesser Antilles (Kalinago)" = "#aadce0",
                                  "Greater Antilles" = "#376795",
                                  "Bahamas (Lucayan)" = "#ef8a47")) +
  scale_fill_manual(values = c("Mainland" = "#A1A1A1",
                                  "Continental islands" = "#663171",
                                  "Lesser Antilles (Kalinago)" = "#aadce0",
                                  "Greater Antilles" = "#376795",
                                  "Bahamas (Lucayan)" = "#ef8a47")) +
  labs(x = "Species richness",
       y = "Euclidean distance from centroid", 
       color = "Subregion", 
       title = "Caribbean") +
  theme_classic() +
  theme(legend.position = "none") +
  coord_cartesian(ylim = c(0.5,13))
```

```{r eval = FALSE}
## Pairwise adonis - PERMANOVA Caribbean ####
Carib.PERMA <- pairwise.adonis2(carib.pca.cwm.values[,8:10] ~ subregion + spp_richness + pred_richness, 
                                data = carib.pca.cwm.values, 
                                by = "margin", # understanding the independent contribution of each variable (marginal effect)
                                method = "euclidean", nperm = 999)
Carib.PERMA # Oceanic archipelagos are way different!

Carib_pairwise_result <- map_dfr(
  Carib.PERMA,
  ~ .x %>%
    as.data.frame() %>%
    tibble::rownames_to_column("Term") %>%
    as_tibble(),
  .id = "Comparison"
) |>
  as.data.frame() |>
  dplyr::select(c(Comparison, Term, R2, `F`, `Pr(>F)`)) |>
  filter(Comparison != "parent_call",
         Term != "Residual",
         Term != "Total") 

print(xtable::xtable(Carib_pairwise_result, digits = 3), include.rownames = FALSE)

saveRDS(Carib.PERMA, "results_funbiog/Carib.PERMA.RDS")
saveRDS(Carib_pairwise_result, "results_funbiog/Carib_pairwise_result.RDS")
```

```{r}
carib.pca.cwm.values$C_m <- carib.pca.cwm.values$spp_richness - carib.pca.cwm.values$pred_richness

carib.pca.cwm.values$P_m <- carib.pca.cwm.values$pred_richness

# compare models
Carib.PC1.mod.Null <- glm(PC1 ~ 1, 
                          data = carib.pca.cwm.values)
Carib.PC1.mod.Subr <- glm(PC1 ~ subregion, 
                       data = carib.pca.cwm.values)
Carib.PC1.mod.Sm <- glm(PC1 ~ C_m, 
                     data = carib.pca.cwm.values)
Carib.PC1.mod.Pm <- glm(PC1 ~ P_m, 
                     data = carib.pca.cwm.values)
Carib.PC1.mod.Sm.Subr <- glm(PC1 ~ C_m*subregion + P_m, 
                          data = carib.pca.cwm.values)
Carib.PC1.mod.Pm.Subr <- glm(PC1 ~ P_m*subregion + C_m, 
                          data = carib.pca.cwm.values)

Carib.PC2.mod.Null <- glm(PC2 ~ 1, 
                          data = carib.pca.cwm.values)
Carib.PC2.mod.Subr <- glm(PC2 ~ subregion, 
                          data = carib.pca.cwm.values)
Carib.PC2.mod.Sm <- glm(PC2 ~ C_m, 
                        data = carib.pca.cwm.values)
Carib.PC2.mod.Pm <- glm(PC2 ~ P_m, 
                        data = carib.pca.cwm.values)
Carib.PC2.mod.Sm.Subr <- glm(PC2 ~ C_m*subregion + P_m, 
                             data = carib.pca.cwm.values)
Carib.PC2.mod.Pm.Subr <- glm(PC2 ~ P_m*subregion + C_m, 
                             data = carib.pca.cwm.values)

Carib.PC3.mod.Null <- glm(PC3 ~ 1, 
                          data = carib.pca.cwm.values)
Carib.PC3.mod.Subr <- glm(PC3 ~ subregion, 
                          data = carib.pca.cwm.values)
Carib.PC3.mod.Sm <- glm(PC3 ~ spp_richness, 
                        data = carib.pca.cwm.values)
Carib.PC3.mod.Pm <- glm(PC3 ~ pred_richness, 
                        data = carib.pca.cwm.values)
Carib.PC3.mod.Sm.Subr <- glm(PC3 ~ spp_richness*subregion + pred_richness, 
                             data = carib.pca.cwm.values)
Carib.PC3.mod.Pm.Subr <- glm(PC3 ~ pred_richness*subregion + spp_richness, 
                             data = carib.pca.cwm.values)

AICcmodavg::aictab(cand.set = list(Carib.PC1.mod.Null,
                                   Carib.PC1.mod.Subr,
                                   Carib.PC1.mod.Sm, 
                                   Carib.PC1.mod.Pm,
                                   Carib.PC1.mod.Sm.Subr,
                                   Carib.PC1.mod.Pm.Subr),
                   modnames = c("Null", "Subregion.PC1","Sm.PC1","Pm.PC1","Sm_Subregion.PC1","Pm_Subregion.PC1"))

AICcmodavg::aictab(cand.set = list(Carib.PC2.mod.Null, 
                                   Carib.PC2.mod.Subr,
                                   Carib.PC2.mod.Sm, 
                                   Carib.PC2.mod.Pm,
                                   Carib.PC2.mod.Sm.Subr,
                                   Carib.PC2.mod.Pm.Subr),
                   modnames = c("Null", "Subregion.PC2","Sm.PC2","Pm.PC2","Sm_Subregion.PC2","Pm_Subregion.PC2"))

AICcmodavg::aictab(cand.set = list(Carib.PC3.mod.Null, 
                                   Carib.PC3.mod.Subr,
                                   Carib.PC3.mod.Sm, 
                                   Carib.PC3.mod.Pm,
                                   Carib.PC3.mod.Sm.Subr,
                                   Carib.PC3.mod.Pm.Subr),
                   modnames = c("Null", "Subregion.PC3","Sm.PC3","Pm.PC3","Sm_Subregion.PC3","Pm_Subregion.PC3"))

summary(Carib.PC1.mod.Sm.Subr)
summary(Carib.PC2.mod.Sm.Subr)
summary(Carib.PC2.mod.Pm.Subr)
summary(Carib.PC3.mod.Sm.Subr)


emtrends(Carib.PC1.mod.Sm.Subr, ~ subregion, var = "C_m") |>
  as.data.frame() |>
  ggplot(aes(x = subregion, color = subregion)) +
  geom_hline(yintercept = 0, lty = 2) +
  geom_hline(yintercept = 0.02196600, lty = 2, color = "#A1A1A1") +
  geom_hline(yintercept = 0.01744766, lty = 3, color = "#A1A1A1") +
  geom_hline(yintercept = 0.02648433, lty = 3, color = "#A1A1A1") +
  geom_pointrange(aes(y = C_m.trend, ymin = lower.CL, ymax = upper.CL), 
                  alpha = 0.8, size = 1) +
  scale_color_manual(values = c("Mainland" = "#A1A1A1",
                                  "Continental islands" = "#663171",
                                  "Lesser Antilles (Kalinago)" = "#aadce0",
                                  "Greater Antilles" = "#376795",
                                  "Bahamas (Lucayan)" = "#ef8a47")) +
  labs(x = "Subregion",
       y = "EMMs of `competitors` effect on PC1 (47.92%)", # pc2 (14.16%) 
       color = "Subregion", 
       title = "Caribbean") +
  coord_flip() +
  theme_classic() +
  theme(legend.position = "none")

emtrends(Carib.PC2.mod.Sm.Subr, ~ subregion, var = "C_m") |>
  as.data.frame() |>
  ggplot(aes(x = subregion, color = subregion)) +
  geom_hline(yintercept = 0, lty = 2) +
  geom_hline(yintercept = 0.017694081, lty = 2, color = "#A1A1A1") +
  geom_hline(yintercept = 0.015460224, lty = 3, color = "#A1A1A1") +
  geom_hline(yintercept = 0.01992794, lty = 3, color = "#A1A1A1") +
  geom_pointrange(aes(y = C_m.trend, ymin = lower.CL, ymax = upper.CL), 
                  alpha = 0.8, size = 1) +
  scale_color_manual(values = c("Mainland" = "#A1A1A1",
                                  "Continental islands" = "#663171",
                                  "Lesser Antilles (Kalinago)" = "#aadce0",
                                  "Greater Antilles" = "#376795",
                                  "Bahamas (Lucayan)" = "#ef8a47")) +
  labs(x = "Subregion",
       y = "EMMs of `competitors` effect on PC2 (14.16%)", 
       color = "Subregion", 
       title = "Caribbean") +
  coord_flip() +
  theme_classic() +
  theme(legend.position = "none")

```

```{r}
## Figures mean per region PC1-PC3 Caribbean ####
names(carib.pca.cwm.values)

# Define new labels
PC_labels_Carib <- c("PC1" = " PC1\n(47.92%)",
                     "PC2" = "PC2\n(14.16%)",
                     "PC3" = "PC3\n(10.54%)")


Shift_Region_Carib <- carib.pca.cwm.values |>
  dplyr::select(cell, Meta_Archipelago, region, subregion, fig_group, 
                spp_richness, pred_richness, PC1, PC2) |>
  pivot_longer(cols = c(PC1, PC2)) |>
  mutate(linetype = case_when(
    # PC1 rules
    name == "PC1" & subregion %in% c("Mainland", "Lesser Antilles (Kalinago)", "Continental islands") ~ "solid",
    name == "PC1" & subregion %in% c("Bahamas (Lucayan)", "Greater Antilles") ~ "dashed",
    
    # PC2 rules
    name == "PC2" & subregion %in% c("Mainland", "Continental islands", "Greater Antilles") ~ "solid",
    name == "PC2" & subregion %in% c( "Lesser Antilles (Kalinago)", "Bahamas (Lucayan)") ~ "dashed",
    
    # # PC3 rules
    # name == "PC3" & subregion == "Mainland" ~ "solid",
    # name == "PC3" & subregion %in% c("Continental islands", "Bahamas (Lucayan)", "Lesser Antilles (Kalinago)", "Greater Antilles") ~ "dashed",
    
    TRUE ~ "solid"  # fallback
  )) |>
  ggplot(aes(x = spp_richness,
             y = value,
             fill = subregion,
             color = subregion)) +
  facet_wrap(~name, scales = "free_y", ncol = 1, 
             labeller = as_labeller(PC_labels_Carib)) +
  geom_point(alpha = 0.2) +
  geom_smooth(aes(linetype = linetype), method = "lm", se = TRUE, alpha = 0.2, lwd = 1) +
  scale_linetype_identity() +
  scale_fill_manual(values = c("#A1A1A1", "#663171", "#aadce0", "#376795", "#ef8a47")) +
  scale_color_manual(values = c("#A1A1A1", "#663171", "#aadce0", "#376795", "#ef8a47")) +
  labs(x = "Species richness",
       y = "Score",
       title = "Shift – Caribbean") +
  theme_classic() +
  theme(legend.position = "left",
        panel.background =element_rect(fill="transparent",colour="black"),
        panel.grid.minor=element_blank(),
        panel.border=element_rect(fill=NA,colour="grey50"),
        strip.background = element_blank())

Shift_Region_Carib
```

### Indo-Pacific

```{r}
orinma.bd <- betadisper(dist(cbind(orinma.pca.cwm.values[,8:10])), 
                       orinma.pca.cwm.values$subregion, type = "centroid")
TukeyHSD(orinma.bd)

orinma.disp_df <- data.frame(distances = orinma.bd$distances,
                      C_m = (orinma.pca.cwm.values$spp_richness - orinma.pca.cwm.values$pred_richness),
                      P_m = orinma.pca.cwm.values$pred_richness,
                      subregion = orinma.pca.cwm.values$subregion)

# compare models
orinma.mod.Null <- glm(distances ~ 1, 
                       data = orinma.disp_df)
orinma.mod.Subr <- glm(distances ~ subregion, 
                      data = orinma.disp_df)
orinma.mod.Sm <- glm(distances ~ C_m, 
                    data = orinma.disp_df)
orinma.mod.Pm <- glm(distances ~ P_m, 
                    data = orinma.disp_df)
orinma.mod.Sm.Subr <- glm(distances ~ C_m*subregion + P_m, 
                         data = orinma.disp_df)
orinma.mod.Pm.Subr <- glm(distances ~ P_m*subregion + C_m, 
                         data = orinma.disp_df)
AICcmodavg::aictab(cand.set = list(orinma.mod.Null, 
                                   orinma.mod.Subr,
                                   orinma.mod.Sm, 
                                   orinma.mod.Pm,
                                   orinma.mod.Sm.Subr,
                                   orinma.mod.Pm.Subr),
                   modnames = c("Null", "Subregion","Sm","Pm","Sm_Subregion","Pm_Subregion"))

summary(orinma.mod.Sm.Subr)
summary(orinma.mod.Pm.Subr)

# Extract effects of each variable
library(emmeans)
orinma.mod.Sm.Subr.eff <- emtrends(orinma.mod.Sm.Subr, ~ subregion, var = "C_m") |>
  as.data.frame()

orinma.mod.Sm.Subr.eff |>
  ggplot(aes(x = subregion, color = subregion)) +
  geom_hline(yintercept = 0, lty = 2) +
  geom_pointrange(aes(y = C_m.trend, ymin = lower.CL, ymax = upper.CL), 
                  alpha = 0.8, size = 1) +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(y = "EMMs of `competitors` effect on Euclidean distance", 
       x = "Subregion", title = "Indo-Pacific") +
  coord_flip() +
  theme_classic() +
  theme(legend.position = "none")

ggplot(orinma.disp_df, 
       aes(x = C_m, 
           y = distances, 
           color = subregion, 
           fill = subregion)) + 
  geom_point(alpha = 0.2) +
  geom_smooth(method = "lm", alpha = 0.2, lwd = 1) +
#  scale_x_log10() +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  scale_fill_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(x = "Species richness",
       y = "Euclidean distance from centroid", 
       color = "Subregion", 
       title = "Indo-Pacific") +
  theme_classic() +
  theme(legend.position = "none") +
  coord_cartesian(ylim = c(0.1,12))

# predators, similar AIC
orinma.mod.Pm.Subr.eff <- emtrends(orinma.mod.Pm.Subr, ~ subregion, var = "P_m") |>
  as.data.frame()

orinma.mod.Pm.Subr.eff |>
  ggplot(aes(x = subregion, color = subregion)) +
  geom_hline(yintercept = 0, lty = 2) +
  geom_pointrange(aes(y = P_m.trend, ymin = lower.CL, ymax = upper.CL), 
                  alpha = 0.8, size = 1) +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(y = "EMMs of `predators` effect on Euclidean distance", 
       x = "Subregion", title = "Indo-Pacific") +
  coord_flip() +
  theme_classic() +
  theme(legend.position = "none")

ggplot(orinma.disp_df, 
       aes(x = pred_richness, 
           y = distances, 
           color = subregion, 
           fill = subregion)) + 
  geom_point(alpha = 0.2) +
  geom_smooth(method = "lm", alpha = 0.2, lwd = 1) +
#  scale_x_log10() +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  scale_fill_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(x = "Predator species richness",
       y = "Euclidean distance from centroid", 
       color = "Subregion", 
       title = "Indo-Pacific") +
  theme_classic() +
  theme(legend.position = "none") +
  coord_cartesian(ylim = c(0.1,12))
```

```{r}
orinma.PERMA <- pairwise.adonis2(orinma.pca.cwm.values[,8:10] ~ subregion + spp_richness + pred_richness, 
                                 data = orinma.pca.cwm.values, 
                                 by = "margin", # understanding the independent contribution of each variable (marginal effect)
                                 method = "euclidean", nperm = 999)
orinma.PERMA 

# using broom
orinma_pairwise_result <- map_dfr(
  orinma.PERMA,
  ~ .x %>%
    as.data.frame() %>%
    tibble::rownames_to_column("Term") %>%
    as_tibble(),
  .id = "Comparison"
) |>
  as.data.frame() |>
  dplyr::select(c(Comparison, Term, R2, `F`, `Pr(>F)`)) |>
  filter(Comparison != "parent_call",
         Term != "Residual",
         Term != "Total") |>
  mutate(R2 = round(R2, 3),
         F = round(F, 3))

print(xtable::xtable(orinma_pairwise_result, digits = 3), include.rownames = FALSE)

saveRDS(orinma_pairwise_result, "results_funbiog/orinma_pairwise_result.RDS")
saveRDS(orinma.PERMA, "results_funbiog/orinma.PERMA.rds")
```

```{r}
orinma.PERMA <- readRDS("results_funbiog/orinma.PERMA.rds")

# compare models
orinma.PC1.mod.Subr <- glm(PC1 ~ subregion, 
                          data = orinma.pca.cwm.values)
orinma.PC1.mod.Sm <- glm(PC1 ~ spp_richness, 
                        data = orinma.pca.cwm.values)
orinma.PC1.mod.Pm <- glm(PC1 ~ pred_richness, 
                        data = orinma.pca.cwm.values)
orinma.PC1.mod.Sm.Subr <- glm(PC1 ~ spp_richness*subregion + pred_richness, 
                             data = orinma.pca.cwm.values)
orinma.PC1.mod.Pm.Subr <- glm(PC1 ~ pred_richness*subregion + spp_richness, 
                             data = orinma.pca.cwm.values)

orinma.PC2.mod.Subr <- glm(PC2 ~ subregion, 
                          data = orinma.pca.cwm.values)
orinma.PC2.mod.Sm <- glm(PC2 ~ spp_richness, 
                        data = orinma.pca.cwm.values)
orinma.PC2.mod.Pm <- glm(PC2 ~ pred_richness, 
                        data = orinma.pca.cwm.values)
orinma.PC2.mod.Sm.Subr <- glm(PC2 ~ spp_richness*subregion + pred_richness, 
                             data = orinma.pca.cwm.values)
orinma.PC2.mod.Pm.Subr <- glm(PC2 ~ pred_richness*subregion + spp_richness, 
                             data = orinma.pca.cwm.values)

orinma.PC3.mod.Subr <- glm(PC3 ~ subregion, 
                          data = orinma.pca.cwm.values)
orinma.PC3.mod.Sm <- glm(PC3 ~ spp_richness, 
                        data = orinma.pca.cwm.values)
orinma.PC3.mod.Pm <- glm(PC3 ~ pred_richness, 
                        data = orinma.pca.cwm.values)
orinma.PC3.mod.Sm.Subr <- glm(PC3 ~ spp_richness*subregion + pred_richness, 
                             data = orinma.pca.cwm.values)
orinma.PC3.mod.Pm.Subr <- glm(PC3 ~ pred_richness*subregion + spp_richness, 
                             data = orinma.pca.cwm.values)

AICcmodavg::aictab(cand.set = list(orinma.PC1.mod.Subr,
                                   orinma.PC1.mod.Sm, 
                                   orinma.PC1.mod.Pm,
                                   orinma.PC1.mod.Sm.Subr,
                                   orinma.PC1.mod.Pm.Subr),
                   modnames = c("Subregion.PC1","Sm.PC1","Pm.PC1","Sm_Subregion.PC1","Pm_Subregion.PC1"))

AICcmodavg::aictab(cand.set = list(orinma.PC2.mod.Subr,
                                   orinma.PC2.mod.Sm, 
                                   orinma.PC2.mod.Pm,
                                   orinma.PC2.mod.Sm.Subr,
                                   orinma.PC2.mod.Pm.Subr),
                   modnames = c("Subregion.PC2","Sm.PC2","Pm.PC2","Sm_Subregion.PC2","Pm_Subregion.PC2"))

AICcmodavg::aictab(cand.set = list(orinma.PC3.mod.Subr,
                                   orinma.PC3.mod.Sm, 
                                   orinma.PC3.mod.Pm,
                                   orinma.PC3.mod.Sm.Subr,
                                   orinma.PC3.mod.Pm.Subr),
                   modnames = c("Subregion.PC3","Sm.PC3","Pm.PC3","Sm_Subregion.PC3","Pm_Subregion.PC3"))

summary(orinma.PC1.mod.Sm.Subr) # 45.65%
summary(orinma.PC2.mod.Pm.Subr) # 23.01%
summary(orinma.PC3.mod.Sm.Subr) #  7.22%


emtrends(orinma.PC1.mod.Sm.Subr, ~ subregion, var = "spp_richness") |>
  as.data.frame() |>
  ggplot(aes(x = subregion, color = subregion)) +
  geom_hline(yintercept = 0, lty = 2) +
  geom_hline(yintercept = 0.00640185, lty = 2, color = "#A1A1A1") +
  geom_hline(yintercept = -0.00034308, lty = 3, color = "#A1A1A1") +
  geom_hline(yintercept = 0.01314679, lty = 3, color = "#A1A1A1") +
  geom_pointrange(aes(y = spp_richness.trend, ymin = lower.CL, ymax = upper.CL), 
                  alpha = 0.8, size = 1) +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(x = "Subregion",
       y = "EMMs of `competitors` effect on PC1 (45.65%)", 
       color = "Subregion", 
       title = "Indo-Pacific") +
  coord_flip() +
  theme_classic() +
  theme(legend.position = "none")

emtrends(orinma.PC2.mod.Pm.Subr, ~ subregion, var = "pred_richness") |>
  as.data.frame() |>
  ggplot(aes(x = subregion, color = subregion)) +
  geom_hline(yintercept = 0, lty = 2) +
  geom_hline(yintercept = -0.1770482, lty = 2, color = "#A1A1A1") +
  geom_hline(yintercept = -0.2930345, lty = 3, color = "#A1A1A1") +
  geom_hline(yintercept = -0.0610620, lty = 3, color = "#A1A1A1") +
  geom_pointrange(aes(y = pred_richness.trend, ymin = lower.CL, ymax = upper.CL), 
                  alpha = 0.8, size = 1) +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(x = "Subregion",
       y = "EMMs of `predators` effect on PC2 (23.01%)", 
       color = "Subregion", 
       title = "Indo-Pacific") +
  coord_flip() +
  theme_classic() +
  theme(legend.position = "none")
```


```{r}
## Figures mean per region PC1-PC2 Indopacific ####
names(orinma.pca.cwm.values)

orinma.pca.cwm.values |>
  mutate(linetype = ifelse(subregion %in% c("Continental islands", 
                                            "New Guinea"), "solid", "dashed")) |>
  ggplot(aes(x = spp_richness, y = PC1, color = subregion, fill = subregion)) +
    geom_point(alpha = 0.2) +
  geom_smooth(aes(linetype = linetype), method = "lm", se = TRUE, alpha = 0.2, lwd = 1) +
  scale_linetype_identity() +
  scale_fill_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(x = "Non-predator species richness",
       y = "PC1",
       title = "Shift – Indo-Pacific") +
  theme_classic() +
  theme(legend.position = "none",
        panel.background =element_rect(fill="transparent",colour="black"),
        panel.grid.minor=element_blank(),
        panel.border=element_rect(fill=NA,colour="grey50"),
        strip.background = element_blank())


orinma.pca.cwm.values |>
  mutate(linetype = ifelse(subregion %in% c("Continental islands", 
                                            "Andaman & Nicobar",
                                            "New Guinea"), "dashed", "solid")) |> 
  ggplot(aes(x = pred_richness, y = PC2, color = subregion, fill = subregion)) +
    geom_point(alpha = 0.2) +
  geom_smooth(aes(linetype = linetype), method = "lm", se = TRUE, alpha = 0.2, lwd = 1) +
  scale_linetype_identity() +
  scale_fill_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  scale_color_manual(values = c("#A1A1A1",
                               "#663171",
                               
                               "#BA3841",
                               "#DF5C2D",
                               "#E6824F",
                               "#B7917F",
                               "#0C7156")) +
  labs(x = "Predator species richness",
       y = "PC2") +
  theme_classic() +
  theme(legend.position = "none",
        panel.background =element_rect(fill="transparent",colour="black"),
        panel.grid.minor=element_blank(),
        panel.border=element_rect(fill=NA,colour="grey50"),
        strip.background = element_blank())


# Define new labels
PC_labels_orinma <- c("PC1" = " PC1\n(45.65%)",
                     "PC2" = "PC2\n(23.01%)")


Shift_Region_orinma <- orinma.pca.cwm.values |>
  dplyr::select(cell, Meta_Archipelago, region, subregion, fig_group, 
                spp_richness, pred_richness, PC1, PC2) |>
  pivot_longer(cols = c(PC1, PC2)) |>
  # mutate(linetype = case_when(
  #   # PC1 rules
  #   name == "PC1" & subregion %in% c("Mainland", "Lesser Antilles (Kalinago)", "Continental islands") ~ "solid",
  #   name == "PC1" & subregion %in% c("Bahamas (Lucayan)", "Greater Antilles") ~ "dashed",
  #   
  #   # PC2 rules
  #   name == "PC2" & subregion %in% c("Mainland", "Continental islands", "Greater Antilles") ~ "solid",
  #   name == "PC2" & subregion %in% c( "Lesser Antilles (Kalinago)", "Bahamas (Lucayan)") ~ "dashed",
  #   
  #   # # PC3 rules
  #   # name == "PC3" & subregion == "Mainland" ~ "solid",
  #   # name == "PC3" & subregion %in% c("Continental islands", "Bahamas (Lucayan)", "Lesser Antilles (Kalinago)", "Greater Antilles") ~ "dashed",
  #   
  #   TRUE ~ "solid"  # fallback
  # )) |>
  ggplot(aes(x = spp_richness,
             y = value,
             fill = subregion,
             color = subregion)) +
  facet_wrap(~name, scales = "free_y", ncol = 1, 
             labeller = as_labeller(PC_labels_orinma)) +
  geom_point(alpha = 0.2) +
  geom_smooth(aes(linetype = linetype), method = "lm", se = TRUE, alpha = 0.2, lwd = 1) +
  scale_linetype_identity() +
  scale_fill_manual(values = c("#A1A1A1", "#663171", "#aadce0", "#376795", "#ef8a47")) +
  scale_color_manual(values = c("#A1A1A1", "#663171", "#aadce0", "#376795", "#ef8a47")) +
  labs(x = "Species richness",
       y = "Score",
       title = "Shift – Caribbean") +
  theme_classic() +
  theme(legend.position = "left",
        panel.background =element_rect(fill="transparent",colour="black"),
        panel.grid.minor=element_blank(),
        panel.border=element_rect(fill=NA,colour="grey50"),
        strip.background = element_blank())

Shift_Region_Carib
```


## How different are the results using presence only data?

```{r}
# global functional traits (after imputation)
functraits_global <- readRDS("FunctionalTraits/birds_functraits_global_imputed.RDS") 

spp_cell_df <- readRDS("results_funbiog/Species_in_cells_MetaArchipelagos.RDS")

spp_cell_df_func <- spp_cell_df |>
  left_join(functraits_global) |>
  group_by(cell, scientific_name) |>
  # mean abundance per species per cell
  mutate(mu_hat = mean(mu_hat, na.rm = TRUE),
         omega_hat = mean(omega_hat, na.rm = TRUE)) |>
  as.data.frame() 

names(spp_cell_df_func)
```

From having for every trait $j$ and sampling unit (cell) $m$ the cell-level trait means as community-weighted mean:

$$
\textit{CWM}_{j,m} = \sum_s^{S_m} r_{s,m} t_{s,j}
$$

\noindent where $S_m$ is the number of species per cell $m$, $r_{s,m}$ is the relative abundance of species $s$ in plot $m$ (computed from the sum of estimated log-abundance mean at stationarity $\mu$), and $t_{s,j}$ is the mean value of species $s$ for trait $j$, achieved with the function `weighted.mean()` in `R`. For presence-only data (every species gets the same weight $r_{s,m} = \frac{1}{S_m}$) the approach could be:

$$
\textit{CWM}_{j,m}^{\text{presence}} = \frac{1}{S_m} \sum_{s}^{S_m} t_{s,m}
$$

The new computation is

```{r}
# Calculating the CWM and CWV

islands_cwm_mu_summary2 <- spp_cell_df_func |>
  group_by(cell, Meta.Archipelago, subregion) |>
  mutate(total_mu_hat = sum(mu_hat)) |> 
  ungroup() |>
  mutate(rel_mu = 1) |> 
  group_by(cell, Meta.Archipelago, subregion) |> 
  summarise(spp_richness = n(),
            pred_richness = sum(`Diet - Vertebrate` > 5, na.rm = TRUE),
          # CWM
            `body mass` = weighted.mean(`log (Body mass)`, rel_mu, na.rm = T),
            `hand wing index` = weighted.mean(`Hand Wing Index`, rel_mu, na.rm = T),
            `tarsus length` = weighted.mean(`Tarsus length`, rel_mu, na.rm = T),
            `wing length` = weighted.mean(`Wing length`, rel_mu, na.rm = T),
            `beak length nar` = weighted.mean(`Beak length (nares)`, rel_mu, na.rm = T), 
            `beak length cul` = weighted.mean(`Beak length (culmen)`, rel_mu, na.rm = T), 
            `beak width` = weighted.mean(`Beak width`, rel_mu, na.rm = T),
            `beak depth` = weighted.mean(`Beak depth`, rel_mu, na.rm = T), 
            `diet breadth` = weighted.mean(`Diet breadth`, rel_mu, na.rm = T), 
            `habitat breadth` = weighted.mean(`Habitat breadth`, rel_mu, na.rm = T),
            `eco speci index` = weighted.mean(`EcoloSpeciIndex`, rel_mu, na.rm = T),
            `verticality` = weighted.mean(`Verticality index`, rel_mu, na.rm = T),
            `elevation range` = weighted.mean(`Elevational range`, rel_mu, na.rm = T),
            `clutch size` = weighted.mean(`Clutch size`, rel_mu, na.rm = T),
            `n broods` = weighted.mean(`Number of broods`, rel_mu, na.rm = T),
            `nest locat breadth` = weighted.mean(`Nest location breadth`, rel_mu, na.rm = T),
            `nest type breadth` = weighted.mean(`Nest type breadth`, rel_mu, na.rm = T),
          # CWM_presence
            `body mass` = `body mass` / spp_richness,
            `hand wing index` = `hand wing index` / spp_richness,
            `tarsus length` = `tarsus length` / spp_richness,
            `wing length` = `wing length` / spp_richness,
            `beak length nar` = `beak length nar` / spp_richness, 
            `beak length cul` = `beak length cul` / spp_richness, 
            `beak width` = `beak width` / spp_richness,
            `beak depth` = `beak depth` / spp_richness, 
            `diet breadth` = `diet breadth` / spp_richness, 
            `habitat breadth` = `habitat breadth` / spp_richness,
            `eco speci index` = `eco speci index` / spp_richness,
            `verticality` = `verticality` / spp_richness,
            `elevation range` = `elevation range` / spp_richness,
            `clutch size` = `clutch size` / spp_richness,
            `n broods` = `n broods` / spp_richness,
            `nest locat breadth` = `nest locat breadth` / spp_richness,
            `nest type breadth` = `nest type breadth` / spp_richness) |>
  as.data.frame()
```

Group for figure

```{r}
islands_cwm_mu2 <- islands_cwm_mu_summary2 |>
  mutate(subregion = ifelse(subregion == "Mainland", "Mainland",
                     ifelse(subregion == "Caribbean - Continental islands", "Continental islands",
                     ifelse(subregion == "Caribbean - Greater Antilles", "Greater Antilles",
                     ifelse(subregion == "Caribbean - Kalinago", "Lesser Antilles (Kalinago)",
                     ifelse(subregion == "Caribbean - Lucayan", "Bahamas (Lucayan)",
                     ifelse(subregion == "Oriental-Indo-Malayan - Andaman & Nicobar", "Andaman & Nicobar",
                     ifelse(subregion == "Oriental-Indo-Malayan - Continental islands", "Continental islands",
                     ifelse(subregion == "Oriental-Indo-Malayan - Philippines", "Philippines",
                     ifelse(subregion == "Oriental-Indo-Malayan - Sunda islands", "Sunda islands",
                     ifelse(subregion == "Oriental-Indo-Malayan - Wallacea", "Wallacea",
                     ifelse(subregion == "Papuan-Melanesian - Bismarcks", "Bismarcks",
                     ifelse(subregion == "Papuan-Melanesian - Papua", "Papua",
                     ifelse(subregion == "Papuan-Melanesian - Solomons", "Solomons",
                     ifelse(subregion == "Papuan-Melanesian - Vanuatu", "Vanuatu",
                     subregion)))))))))))))),
         region = ifelse(Meta.Archipelago == "Indo.Pacific", "Indo Pacific",
                                   "Caribbean"),
         fig_group = ifelse(subregion == "Mainland", "Mainland",
                     ifelse(subregion == "Continental islands", "Islands",
                     ifelse(subregion == "Greater Antilles", "Islands",
                     ifelse(subregion == "Lesser Antilles (Kalinago)", "Islands",
                     ifelse(subregion == "Bahamas (Lucayan)", "Islands",
                     ifelse(subregion == "Andaman & Nicobar", "Islands",
                     ifelse(subregion == "Philippines", "Islands",
                     ifelse(subregion == "Sunda islands", "Islands",
                     ifelse(subregion == "Wallacea", "Islands",
                     ifelse(subregion == "Bismarcks", "Islands",
                     ifelse(subregion == "Papua", "Islands",
                     ifelse(subregion == "Solomons", "Islands",
                     ifelse(subregion == "Vanuatu", "Islands",
                     subregion))))))))))))))

names(islands_cwm_mu2)

```

```{r}
caribbean_cwm2 <- islands_cwm_mu2 |>
  filter(Meta.Archipelago %in% "Caribbean") |>
  # bring back the mean value of longitude for each cell
  left_join(spp_cell_df |> 
              dplyr::select(cell,longitude, latitude) |> 
              group_by(cell) |> 
              summarize(longitude = mean(longitude),
                        latitude = mean(latitude))) |>
  filter(latitude < 24 | longitude > -79.5, 
         spp_richness >= 15) # 1st Q of species richness in the Caribbean

names(caribbean_cwm2)

caribbean_cwm2[,c(3, 6:22)] |>
  pivot_longer(cols = !c(subregion), names_to = "Trait",values_to = "CWM") |>
  ggplot(aes(x = scale(log10(CWM)))) + geom_histogram() +facet_wrap(~Trait, scales = "free")+theme_classic()

funspaceDim(scale(log10(caribbean_cwm2[,c(6:22)]))) # 4 dimensions

carib.pca.cwm2 <- prcomp(scale(log10(caribbean_cwm2[,c(6:22)])))
carib.pca.cwm.values2 <- data.frame(cell = caribbean_cwm2$cell,
                               Meta_Archipelago = caribbean_cwm2$Meta.Archipelago,
                               region = caribbean_cwm2$region,
                               subregion = caribbean_cwm2$subregion,
                               fig_group = caribbean_cwm2$fig_group,
                               spp_richness = caribbean_cwm2$spp_richness,
                               pred_richness = caribbean_cwm2$pred_richness,
                               carib.pca.cwm2$x)

carib.pca.cwm.loadings2 <- data.frame(Variables = rownames(carib.pca.cwm2$rotation), 
                                     carib.pca.cwm2$rotation)

aload.car <- abs(carib.pca.cwm$rotation)
View(sweep(aload.car, 2, colSums(aload.car), "/"))

aload.car2 <- abs(carib.pca.cwm2$rotation)
View(sweep(aload.car2, 2, colSums(aload.car2), "/"))

summary(carib.pca.cwm)
summary(carib.pca.cwm2)

```

```{r}
library(plotly)
p

# Define your palette
subregion_colors <- c(
  "Mainland"            = "#A1A1A1",
  "Continental islands" = "#663171",
  "Lesser Antilles (Kalinago)"     = "#aadce0",
  "Greater Antilles"    = "#376795",
  "Bahamas (Lucayan)"             = "#ef8a47"
)

# Scale richness to a reasonable marker size range
size_scale2 <- scales::rescale(carib.pca.cwm.values2$spp_richness, to = c(4, 18))

# Base scatter plot with relative size
p2 <- plot_ly(
  data = carib.pca.cwm.values2,
  x = ~PC1, y = ~PC2, z = ~PC3,
  type = "scatter3d", mode = "markers",
  color = ~subregion,
  colors = subregion_colors,
  marker = list(
    size = size_scale2,   # relative size by richness
    opacity = 0.5
  ),
  text = ~paste("Richness:", spp_richness) # hover info
)

# Add loading vectors for top contributors
carib.pca.cwm.loadings2$contrib <- rowSums(carib.pca.cwm.loadings2[, c("PC1","PC2","PC3")]^2)
top_vars2 <- carib.pca.cwm.loadings2[order(-carib.pca.cwm.loadings2$contrib), ][1:10, ]

for (i in seq_len(nrow(top_vars2))) {
  v <- top_vars2[i, ]
  
  # Arrow (line only)
  p2 <- add_trace(
    p2,
    x = c(0, v$PC1 * 2),
    y = c(0, v$PC2 * 2),
    z = c(0, v$PC3 * 2),
    type = "scatter3d", mode = "lines",
    line = list(color = "black", width = 4),
    showlegend = FALSE,
    inherit = FALSE
  )
  
  # Label (only at arrow tip)
  p2 <- add_trace(
    p2,
    x = v$PC1 * 2,
    y = v$PC2 * 2,
    z = v$PC3 * 2,
    type = "scatter3d", mode = "text",
    text = v$Variables,
    textposition = "top middle",
    showlegend = FALSE,
    inherit = FALSE
  )
}

p2
```
